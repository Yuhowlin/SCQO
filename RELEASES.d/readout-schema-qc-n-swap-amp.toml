name = "readout-schema-qc-n-swap-amp"
repos = { scqo = "ce50212", scqat = "de6dc06", lchqmdriver = "a3ca6bb", lchqbdriver = "c9aadfb" }
kind = "breaking"
coupling = "lockstep, all four repos at these commits: SCQO exports joint_state_labels + states_to_joint_population that BOTH drivers import, and SCQO pair estimate() requires scqat's joint_population estimators. Failures are LOUD (ImportError / ContractError), no silent-downgrade path. BEHAVIOR CHANGE: pre-cutover pair-run datasets (p_high/p_low/p_ee) can no longer be re-analysed — the reconstruction was deleted."
validated = "QM hardware 5Q4C q1_q2 (qc_n_swap_amp, 2026-08-06, user-confirmed); Qblox decode + single-qubit population rename offline only; all four full suites green (scqat 483 / SCQO 660 / LCHQM 266 / LCHQB 241) + simulated end-to-end smoke"
notes = "BEHAVIOR CHANGE: readout output is now DECLARED, not inferred — 'state' is always a per-shot integer level, 'population' the averaged marginal, 'joint_population' a multi-qubit target's joint distribution (marginals = partial trace). UPGRADE REQUIRES all four repos together. Adds the qc_n_swap_amp experiment (record-only swap error-amplification map, both readout modes). Schema: SCQO TUTORIAL section 11; partial-swap workflow: section 12."
