name = "setup-export"
repos = { scqo = "bf5077f" }
kind = "additive"
coupling = ""   # openpyxl lives in scqo's OWN viewer extra; matplotlib floor unchanged (via scqat)
validated = "offline (test_viewer.py 52 green + rendered-sample inspection 2026-08-14)"
notes = "Setup pages export /setup/<dev>/<cd>/<setup>/export.{html,xlsx,pdf}: self-contained offline HTML (embedded run/campaign snapshots, no data-drive needed by recipients), two-sheet xlsx (Calibration + Physical parameters; entity/parameter/value/unit/source), 16:9 PDF slides (one page per section x source kind). UPGRADE REQUIRES: reinstall viewer venv extras — new openpyxl dep — e.g. BLUEFORSAS2 .venv-view: uv pip install -e <SCQO>[viewer]."
