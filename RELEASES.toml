# Release manifest — which tag of each repo belongs to a release combo.
# The server checks out ONE release name across the scqo-versioned repos and takes
# scqat at its pinned tag (scqat versions independently: its own pyproject/tag line).
# Hand-edited at release time by the manager (see RELEASING.md).
#
# PENDING features live in RELEASES.d/ — ONE fragment file per feature (format +
# multi-agent rules in RELEASES.d/README.md). Appended by whoever lands a feature;
# consumed into the next [vX-Y-Z] block and DELETED at release (RELEASING.md step 0).

[v3-0-0]
date = 2026-08-19
scqo = "v3.0.0"                  # BREAKING detuning-window frames (drive + readout) + resonator fact renames; ADDITIVE broadband search pair, coupling g_coeff, punchout branches, cryoscope drive shape
scqo-qm = "v3.0.0"               # broadband probes (multi-band MW-FEM) + cryoscope run-scoped drive op + detuning-window tests
scqo-qblox = "v3.0.0"            # endpoint-form NCO sweeps for BOTH detuning windows (LOCKSTEP with scqo — see notes)
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged, incompatible since greenfield)
scqat = "v0.19.0"                # punchout two-band branches + f_bare pin/E_c arch + robust_noise gate + broadband estimators — REQUIRED floor, see notes
notes = "BREAKING COMBO — ten features (3 breaking / 5 additive / 2 fix), the largest cut since v1; the three breaking fragments all rename things an operator owns. UPGRADE REQUIRES, in order: (1) DETUNING WINDOWS (drive + readout, both frames in the FIELD NAME): the four qubit_spectroscopy* experiments retire frequency_span_hz/num_points (qubit_spectroscopy, _overlap), frequency_span_hz (_flux_pulse) and min/max_detuning_hz (_cryoscope) for start_drive_detuning_hz/end_drive_detuning_hz/num_drive_freq_points, and the five readout sweeps (resonator_spectroscopy, both punchouts, resonator_spectroscopy_flux, readout_frequency) retire frequency_span_hz + num_points/num_freq_points for start_readout_detuning_hz/end_readout_detuning_hz/num_readout_freq_points — update standing parameters.toml overlays, campaign plans and scripts; a stale key is REFUSED at run() (the catalog overlay skips it silently, so --help looks fine and the run does not). Windows may be ASYMMETRIC and the edges take EITHER order; the axis is always emitted ascending because scqat's peak_fit mis-fits a descending axis SILENTLY. The operator parameters.toml on the dev machine is already migrated. (2) RESONATOR FACT RENAMES: f_r0_hz -> f_bare_hz and f_r_hz -> f_dress0_hz in every physical.json and design.toml (values unchanged; a cavity's f_r_hz is NOT renamed) — ALREADY APPLIED to 5Q4C (cd1, cd2) and chipA (cd1 x3) plus both design.toml; any OTHER device folder must be migrated before running. Delete any f_q_max_hz key from [resonator_spectroscopy_flux] tables (Parameter retired, extra=forbid refuses). (3) LOCKSTEP FLOOR, SILENT THE WRONG WAY: scqo-qblox MUST move to v3.0.0 with scqo — a pre-v3 qblox shell against v3 scqo re-derives a symmetric center+/-span/2 NCO sweep, so an ASYMMETRIC window is quietly re-centered and every fitted frequency is attributed wrong; it bites hardest on the punchout, which walks the dip DOWN from f_dress0 toward f_bare. scqo-qm is safe either way (probes consume sweep_axes as given). (4) scqat PIN v0.19.0 is a hard floor four ways: resonator_spectroscopy_flux passes f_r0/fit_f_r0/ec/g_init kwargs (ValueError under older), both punchouts read the branch keys (KeyError), the broadband pair lazy-imports its estimators (ImportError), and cryoscope's smooth envelopes need the robust_noise gate (SILENT n_peaks_found=0 under the old value-MAD on a wide line). (5) EDITABLE REINSTALLS are REQUIRED (pyproject versions bumped in all four repos; .dist-info freezes at install time): uv pip install --python <venv>\\Scripts\\python.exe -e D:\\github\\SCQO -e D:\\github\\scqat --no-deps per venv (.venv-qm, .venv-qblox, .venv-view), plus each driver editable into its own venv. WHAT SHIPS: BREAKING x3 — drive_detuning + readout_detuning capabilities (explicit [start,end] windows relative to the standing frequency, catalog-browsable via scqo run --capability) and the resonator frequency family rename (f_dress1_hz reserved for the |1> dip -> chi; resonator_spectroscopy_flux now auto-sources f_q_max via drive_freq_hz -> Ambegaokar-Baratoff junction resistance -> design -> assumed, pins a MEASURED f_bare_hz, and new transmon facts ec_hz/junction_resistance_ohm/gap_delta_hz land in the catalog). ADDITIVE x5 — the BROADBAND SEARCH PAIR broadband_qubit_spectroscopy / broadband_resonator_spectroscopy (contributed by Yuhowlin; record-only wideband bring-up scans over an ABSOLUTE [start,stop] span by stepped LO sub-bands; QM multi-band MW-FEM support switches opx_output.band per segment and drags each target's PORT-PAIR PARTNER along parked at IF=0, restored in finally — on 5Q4C scanning q1 takes q2 along, q3 takes q4, q5 is partner-free; probe_self_acquires so --preview REFUSES both; drive_power_dbm defaults to -25 dBm vs the -40 the validation used — set it explicitly; KNOWN GAPS shipped deliberately and tracked in the PR follow-ups: no qubit-estimator/sim tests, the neutral bandwidth_per_lo_hz/lo_gap_hz semantics differ from the QM probe's derived 200 MHz spans and LO placement, default_targets() now also filters by required_operations, and qubit_tomography gained reset_frame() after reset in the same PR); COUPLING g RECORDED TWICE (g_hz at the idle point + the NEW dimensionless geometry constant g_coeff = g_hz/sqrt(f_q*f_bare), both punchouts and the flux map write it, routes agree to 0.2-2.4% at c~0.0182 on three qubits; both punchouts now propose SIX values and the flux map SEVEN — blind accepts write the coupling pair; DO NOT extrapolate g_coeff down a flux arch, refuted on 13 runs); PUNCHOUT TWO-BAND BRANCH EXTRACTION v2 (f_bare_hz + f_dress0_hz + lamb shift + plateau boundary powers from anchored two-band classification, HW-validated on QM 5Q4C q3, v1 was falsified by the first hardware runs — the global FWHM gate deleted the bare plateau; Qblox live run owed); FLUX DESIGN-g SEED RESCALE (design-tier g seed rescaled to chip frequencies; g(phi) INSIDE the model was implemented and REJECTED by data — do not re-attempt without new evidence); SPECTROSCOPY-CRYOSCOPE DRIVE SHAPE (drive_shape square|cosine|gaussian built per run as a run-scoped op dropped in finally; THE DEFAULT IS SQUARE ON MEASUREMENT — center scatter tracks LINEWIDTH alone at ~0.009 MHz/MHz across envelopes, so the narrowest line wins and shape is NOT the lever, length is; reach for smooth envelopes only against a line that looks WRONG). FIX x2 — qubit_relaxation no longer crashes on a flat trace (failed fit reports success=false + raw-data figure), and peak_fit's min_snr gate measures point-to-point noise (DER_SNR) instead of value-MAD, so a line filling >40% of a window is DETECTABLE again (previously silent n_peaks_found=0; re-analyse any suspicious no-peak runs — two previously-unfittable 5Q4C cryoscope runs now fit, the previously-passing one is bit-identical). HARDWARE LEDGER: VALIDATED THIS CUT — broadband qubit variant (5Q4C q1 run 20260819-151504-090, candidate 5.1438 GHz vs stored 5.14198 GHz), punchout v2 (QM q3), cryoscope drive shapes (3 runs), coupling-coefficient cross-check (13 runs). STILL OWED — broadband RESONATOR variant + the cross-band SWITCHING path (offline-only; pre-flighted through generate_config() on the real 5Q4C cd2 state at every slice, never executed), punchout v2 on Qblox, coupling-coefficient live run of the new code path, readout average mode on both backends (v2.2.0), register_stark.py + qc_n_stark_amp (v2.0/2.1). OFFLINE-VALIDATED at the cut: scqat 623, scqo full suite + 3-OS CI green, scqo-qm 328 (run with the shared .venv-qm; the repo .venv is missing typing_extensions and needs a rebuild from requirements-qm.lock.txt), scqo-qblox 273. SERVER UPGRADE (BLUEFORSAS2): INSTALL section 5 — git fetch --tags && git checkout v3.0.0 in scqo/scqo-qm/scqo-qblox + scqat at v0.19.0, editable reinstalls per venv as above (REQUIRED this cut), restart the viewer, then scqo doctor from a NON-INSTALLER account over SSH. Fixed-frequency devices: chipA state was already fact-renamed; re-check root __class__=FixedFrequencyQuam after any QUAM save."

[v2-3-0]
date = 2026-08-17
scqo = "v2.3.0"                  # ADDITIVE catalog `tags` -> `capabilities` rename + the `scqo run --capability` browser
scqo-qm = "v2.3.0"               # no-change re-tag (no commits since v2.2.0)
scqo-qblox = "v2.3.0"            # no-change re-tag (no commits since v2.2.0)
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged, incompatible since greenfield)
scqat = "v0.18.0"                # unchanged pin — this combo introduces no scqat floor
notes = "ADDITIVE COMBO — one scqo-only feature; nothing an operator owns changes (no Parameters, experiment names, state files, config keys or stored state) and no dependency ships. CLASSIFICATION NOTE: the capability-rename-and-browse fragment was filed `breaking` — RELEASING.md's version rule lists renamed catalog fields as breaking — and was judged ADDITIVE at the cut: the renamed key is a Python dict key on catalog() with ZERO consumers anywhere in the stack (verified across scqo-qm, scqo-qblox, scqo-contrib, scqat and the operator's notebooks), so no user artifact must change to keep working. What does change is that dict key and the bare `scqo run` output format, both under BEHAVIOR CHANGE below. Same re-judgment precedent as v2.2.0's readout-average-mode fragment. ADDITIVE (scqo) — THE CATALOG'S DERIVED MARKERS ARE NOW `capabilities`, NEVER `tags`: catalog() entries carry `capabilities`, `_derived_tags` became `_derived_capabilities`, and `scqo run <name> --help` prints a `capabilities:` line. The word `tag` now means exactly ONE thing across the stack — the datastore's user-attached RUN tags (record.json + index.sqlite, `scqo tag`, `scqo find --tag`, `scqo run --tag`, config default_tags, the design-seeded `seeded:` anchors), all UNCHANGED. The collision was real enough that every discussion of either had to disambiguate first, and the asymmetry decided which side moved: capabilities are recomputed from Parameters-mixin subclassing on every catalog() call and persist NOWHERE, so renaming them costs no migration, while run tags live in immutable run records and keep the word. Capabilities stay DERIVED and never declared; zero capabilities stays legitimate. ADDITIVE (scqo) — CATALOG BROWSING ON THE ONE ENTRY POINT: bare `scqo run` now prints compact name COLUMNS (column-major, so the alphabetical families read DOWN each column) closed by a capability-count footer, and the new `scqo run --capability NAME` filters that listing — repeatable = AND, and `none` is the zero-capability bucket (legitimate for a not-yet-classifiable experiment, so it must stay browsable). Backed by a curated CAPABILITY_SUMMARIES table in _capabilities whose keys are pinned to the DERIVABLE set by test_capabilities, so the browser can never advertise a capability that does not exist. Guards refuse an experiment name alongside the flag, an unknown capability (the error lists the valid set) and `none` AND-ed with a real capability — all before a session is built. No new verb: `scqo run` remains the single CLI entry point. BEHAVIOR CHANGE (scqo) — (1) any consumer reading `entry['tags']` off catalog() must read `entry['capabilities']`; no shim or alias ships (house rule) and no such consumer exists in the stack. (2) Bare `scqo run` NO LONGER prints descriptions or per-name capability brackets — the 32-char name column broke on every multi-capability suffix — so a script parsing that output must move to `scqo run <name> --help` (full description + parameter schema) or to `--capability` (the filter); the footer still names every capability and its count. Every CLI meta line is ASCII: an em-dash in the filter header rendered as mojibake on the Windows console and was replaced. OFFLINE-VALIDATED — full suites green at the cut (scqo 798, scqo-qm 318, scqo-qblox 268). NO hardware validation is owed for this combo: the change touches only the catalog surface and the CLI listing — no probe, schedule, program or instrument path. TEST-ORDER NOTE: the new catalog-listing spot-checks passed alone and FAILED in the full suite, because other test modules @register deliberately-broken fixtures at module level and those carry no capabilities, so every one of them piled into the `none` bucket; both spot-checks now enumerate the EXPORTED experiments, the same selection-by-type guard test_capabilities already used. STILL OWED (carried forward, untouched by this combo): register_stark.py run + calibrate and qc_n_stark_amp HARDWARE VALIDATION (v2.0.0/v2.1.0); readout average mode HARDWARE VALIDATION on both backends (v2.2.0). SERVER UPGRADE (BLUEFORSAS2): INSTALL section 5 — git fetch --tags && git checkout v2.3.0 in scqo/scqo-qm/scqo-qblox + scqat at v0.18.0. No new dependency ships, so the [viewer] extra needs NO extras reinstall this time, but DO re-run the editable installs or `scqo doctor` keeps reporting 2.2.0 (editable .dist-info metadata is frozen at install time and a checkout never refreshes it): uv pip install --python <venv>\\Scripts\\python.exe -e D:\\github\\SCQO -e D:\\github\\scqat --no-deps, per venv (.venv-qm, .venv-qblox, .venv-view), restart the viewer, then `scqo doctor` from a NON-INSTALLER account over SSH."

[v2-2-0]
date = 2026-08-17
scqo = "v2.2.0"                  # ADDITIVE per-setup exports (html/xlsx/pdf) + the readout sweeps' average mode
scqo-qm = "v2.2.0"               # readout_power / readout_frequency realize the average readout mode
scqo-qblox = "v2.2.0"            # readout_power / readout_frequency realize the average readout mode
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged, incompatible since greenfield)
scqat = "v0.18.0"                # readout_fidelity average method + merged separation figure — REQUIRED for the readout sweeps, see notes
notes = "ADDITIVE COMBO — two features; nothing an operator owns changes (no Parameters, state files, config keys or experiment names), and the ONE upgrade action is the viewer venv's extras reinstall. CLASSIFICATION NOTE: the readout-average-mode fragment was filed `breaking` for its figure rename and was judged ADDITIVE at the cut — what changes is the readout figure SET and the replot of an OLD plotdata.nc, both covered under BEHAVIOR CHANGE below. ADDITIVE (scqo) — PER-SETUP EXPORTS at /setup/<dev>/<cd>/<setup>/export.{html,xlsx,pdf}, sharing the setup page's own row assembly (setup_page refactored onto _setup_context, behavior-identical): export.html is ONE self-contained file for a recipient with no data drive and no viewer (source links are in-page anchors to EMBEDDED run/campaign snapshots — outcome badges, fit tables, figures as base64 data: URIs; the previous column and every server/file link are excluded by design), export.xlsx is two sheets (Calibration + Physical parameters; entity/parameter/value/unit/source, native numerics), and export.pdf is a 16:9 DOCUMENT TWIN of the offline HTML — one loader, two renderers, the same sections in the same order with continuous tables and the referenced runs' figures (the first cut's kind-sliced slides were rejected in review). Lazy Agg matplotlib, the _campaign_plot discipline. UPGRADE REQUIRES: reinstall the viewer venv's extras — openpyxl is a NEW dependency of the [viewer] extra, and without it the xlsx route answers 503 with the reinstall hint (BLUEFORSAS2: .venv-view). ADDITIVE (scqo + scqo-qm + scqo-qblox + scqat) — readout_mode='average' ON readout_power AND readout_frequency, which were per-shot by construction. The probes now realize BOTH modes off one program: on QM only the stream terminal moves (.buffer(num_shots) -> .average()), on Qblox only the repetition loop's variable stops being captured into the bin coords (labeled = a bin per shot; unlabeled = one averaged bin, the resonator_spectroscopy idiom — pinned on the COMPILED acquisition mapping, 5x2xnum_shots bins vs 5x2). shot_idx moves out of Contract.sweeps into readout_dims with the averaged form as the alt set. With nothing fitted there is NO fidelity: the optimum is the largest blob SEPARATION, best_fidelity lands NaN, and a new best_separation fit key carries the metric actually optimized (recorded in BOTH modes). The scqat method is DERIVED from readout_mode (_capabilities.discrimination_method), so 'averaged data + mixture fit' stays unrepresentable. The DEFAULT STAYS 'shot' and that path is unchanged. CAVEAT: readout_power in average mode maximizes separation, which keeps growing past the onset of measurement-induced transitions — the outlier check that catches those is GMM-only — so use average mode as the fast coarse scan and calibrate the amplitude in shot mode; readout_frequency has no such asymmetry (separation peaks at the optimal detuning). REQUIRES scqat >= v0.18.0 for these two experiments: estimate() passes method= to the readout_fidelity estimator and an older scqat refuses it BY NAME (loud ValueError at estimate; every other experiment is unaffected). BEHAVIOR CHANGE (figures, scqat) — the readout-sweep figure set loses `std`, `mean_distance`, `mean_I` and `mean_Q` and gains ONE merged `separation` figure: the centre separation and the GMM std are the same IQ quantity, so they now share one axes with the y axis anchored at 0 (`snr` and `outlier` also start at 0; `fidelity` and `norm_res` deliberately do not). Average mode draws separation + means_on_IQ only. Run folders are immutable and keep the PNGs they were written with, but an OLD *_plotdata.nc NO LONGER REPLOTS (it carries no `separation` variable) — re-analyse from dataset.nc instead, and any script naming the old PNGs must switch to <estimator>_separation.png. scqat 93fad3d (the mean_I/mean_Q removal) had NO ledger fragment — recorded here at release time. OFFLINE-VALIDATED ONLY: full suites green at the cut (scqo 784, scqat 596, scqo-qm 318, scqo-qblox 268) and both modes smoked end-to-end on the simulated backend, but AVERAGE MODE IS HARDWARE-UNVALIDATED ON BOTH BACKENDS — offline proves the programs build and the datasets conform, not that the instrument averages where we think it does. STILL OWED from v2.0.0/v2.1.0: register_stark.py run + calibrate and qc_n_stark_amp HARDWARE VALIDATION. scqo-qblox rejoins the combo pyproject numbering (it sat at 1.0.1 through three no-change re-tags; it ships code this time). scqo-contrib stays v0.12.0 and is NOT in the combo (incompatible since greenfield). SERVER UPGRADE (BLUEFORSAS2): INSTALL section 5 — git fetch --tags && git checkout v2.2.0 in scqo/scqo-qm/scqo-qblox + scqat at v0.18.0, then re-run the editable installs per venv (.venv-qm, .venv-qblox, .venv-view; these uv-managed venvs have NEITHER pip NOR uv inside them, so drive it from outside: uv pip install --python <venv>\\Scripts\\python.exe -e D:\\github\\SCQO -e D:\\github\\scqat --no-deps) AND for .venv-view reinstall the viewer EXTRA so openpyxl lands (uv pip install --python D:\\github\\.venv-view\\Scripts\\python.exe -e D:\\github\\SCQO[viewer]), restart the viewer, then `scqo doctor` from a NON-INSTALLER account over SSH."

[v2-1-0]
date = 2026-08-14
scqo = "v2.1.0"                  # ADDITIVE qubit_stark_phase_echo + doctor multi-account-server witness
scqo-qm = "v2.1.0"               # qubit_stark_phase_echo probe + single-target preview for pair shells + qc_n_stark_amp gap ordering
scqo-qblox = "v2.1.0"            # no code change; combo version-track re-tag (tree-identical to v2.0.0, pyproject stays 1.0.1)
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged, incompatible since greenfield)
scqat = "v0.17.0"                # qubit_stark_phase_echo estimator — REQUIRED for that experiment, see notes
notes = "ADDITIVE COMBO — three features plus one sequencing fix; nothing breaks and there is NO upgrade action beyond the routine checkout + editable reinstall. ADDITIVE (scqo + scqo-qm + scqat) — qubit_stark_phase_echo, the record-only QM-only AC-STARK PHASE ECHO: y90 - wait(D) - x180 - off-resonant stark(D) - {x90 | -y90} - readout, with BOTH arms at the registered `stark` operation's OWN baked length D (arm 1's idle is derived from it). There is deliberately NO dynamic-duration override — the stark waveform is arbitrary and QUA would ZERO-PAD it, leaving free evolution in arm 2; to change D, re-register the op (quam_config/register_stark.py) and arm 1 follows. The tone rides an update_frequency bracket (base_if + stark_detuning_hz) restored to resonance before the closing pulse and the readout, and only the tone AMPLITUDE is swept. Two closing bases (meas_basis: x90 -> <Z> = sin phi, -y90 -> cos phi, the latter realized as y90 with a negated amplitude_scale) let the estimator recover phi = atan2(sin, cos) unambiguously, and the y90 PREP anchors the phi=0 reference so the measured ABSOLUTE phase is the Stark-induced phase (nothing to subtract). The scqat estimator circle-fits the (cos, sin) quadratures and measures the phase about the FITTED CENTER (offset-robust, not a single-anchor subtraction), anchors phi=0 at the smallest |amplitude|, then fits phi vs amp^2 -> the Stark coefficient; phasor figure in expectation-value coordinates (<X>, <Y> = 1 - 2P). Record-only: no writeback, no catalog change. REQUIRES scqat >= v0.17.0 — the estimator (scqat.estimators.qubit_stark_phase_echo) is lazy-imported inside estimate(), so the catalog imports fine under an older scqat but `scqo run qubit_stark_phase_echo` fails LOUDLY with ImportError at estimate (this ONE experiment only; every other experiment is unaffected). HARDWARE-VALIDATED on 5Q4C 2026-08-13 (run 20260813-011304). ADDITIVE (scqo) — `scqo doctor` gains the MULTI-ACCOUNT-SERVER WITNESS: it WARNs when the running venv's base interpreter is profile-resident (pyvenv.cfg home / sys._base_executable under a C:\\Users\\<someone> profile) and when the resolved lab config or data_root lives under a DIFFERENT account's profile — the uv-trampoline trap that passes for the installer and dies for every other SSH account before Python even starts. New [WARN] rows only; no existing behavior changes and warnings still exit 0. INSTALL section 5 gains the SERVER LAYOUT CONTRACT (nothing shared under C:\\Users: the D:\\github stack, the D:\\uv\\python interpreters via Machine-scope UV_PYTHON_INSTALL_DIR, the D:\\scqo config and the data_root all live on the neutral drive; profiles hold only ~/.scqo personal files), the section-1 explicit patch-dir venv form is promoted to REQUIRED on servers, and every tag upgrade now ends with the acceptance gate: `scqo doctor` from a NON-INSTALLER account over SSH. ADDITIVE (scqo-qm; NO ledger fragment was written for this feature — recorded here at release time) — `scqo run <name> --preview` now renders a SELF-ACQUIRING PAIR SHELL when it exposes a preview_program() hook AND exactly one --target is given: the single target's program is built (no acquire) and rendered as usual. qc_n_stark_amp and qc_n_swap_amp opt in (each probe() factored into a shared _build_pair_program); multi-target or hook-less self-acquiring shells still refuse BY NAME, and the other four self-acquiring shells are unchanged. FIX (scqo-qm) — qc_n_stark_amp's round body now idles gap_cycles BETWEEN the swap and the stark tone (swap -> gap -> stark, was swap -> stark -> gap), so the swap's flux pulse settles BEFORE the off-resonant tone plays; sequencing only — the dynamic loop bound, the swept amplitude and the readout are unchanged. STILL OWED from v2.0.0: register_stark.py run + calibrate and qc_n_stark_amp HARDWARE VALIDATION — treat qc_n_stark_amp results as unvalidated until then, and note that this release CHANGES its pulse ordering, so any earlier offline comparison is superseded. scqo-qblox = no-change version-track re-tag (tree-identical to v2.0.0; its pyproject stays 1.0.1). scqo-contrib stays v0.12.0 and is NOT in the combo (incompatible since greenfield). Offline suites green at the cut: scqo 771, scqat 588, scqo-qm 317, scqo-qblox 266 (the scqo full suite caught one gap the stark-echo landing left — its capability-census entry — pinned here; tests only, no shipped behavior). SERVER UPGRADE (BLUEFORSAS2): INSTALL section 5 — git fetch --tags && git checkout v2.1.0 in scqo/scqo-qm/scqo-qblox + scqat at v0.17.0, then re-run the editable installs per venv (.venv-qm, .venv-qblox, .venv-view; these uv-managed venvs have NEITHER pip NOR uv inside them, so drive it from outside: uv pip install --python <venv>\\Scripts\\python.exe -e D:\\github\\SCQO -e D:\\github\\scqat --no-deps), restart the viewer, then `scqo doctor` from a NON-INSTALLER account over SSH — the new witness rows ARE the acceptance gate."

[v2-0-0]
date = 2026-08-11
scqo = "v2.0.0"                  # BREAKING per-context history.sqlite change-history + viewer restructure; ADDITIVE qc_n_stark_amp
scqo-qm = "v2.0.0"               # qc_n_stark_amp QM probe + register_stark.py
scqo-qblox = "v2.0.0"            # no code change; combo version-track re-tag (tree-identical to v1.0.2, pyproject stays 1.0.1)
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged, incompatible since greenfield)
scqat = "v0.16.0"                # qc_n_stark_amp estimator — REQUIRED for that experiment, see notes
notes = "FIRST POST-1.0 MAJOR — the bump is driven by the breaking change-history restructure (RELEASING.md full SemVer: breaking -> x+1). Two features ship. BREAKING (scqo) — CHANGE HISTORY MOVED OFF JSONL INTO PER-CONTEXT scqo/history.sqlite TRUTH DATABASES (fresh start, NO migration; the old *.history.jsonl sidecars are dead files — delete at leisure; provenance reads 'unrecorded' until the first new change; per-CONTEXT so multi-server aggregation stays a pure folder copy, one writing machine per context), and the VIEWER RESTRUCTURED: per-setup pages under every cooldown cycle (current + previous-run link per parameter), a device-page physical-facts matrix, and change-history-driven /trends (?entity=&field=; port 1 = latest 50 changes in one setup, port 2 = one fact across all contexts). DataStore.fit_trend + report.REPORTABLE_QUANTITIES are DELETED (fit-based trends replaced; report.py gains catalog-derived FIELD_UNITS). UPGRADE REQUIRES: upgrade EVERY state-writing machine together (an old-version session keeps writing sidecars the new code never reads, so last-wins is unreliable while versions are mixed); NEVER delete history.sqlite (it is truth, not the rebuildable index); robocopy aggregation lines add /XF *.sqlite-wal *.sqlite-shm (INSTALL 'Merging data from another server'). ADDITIVE (scqo + scqo-qm + scqat) — qc_n_stark_amp, the record-only QM-only AC-Stark N-swap amplitude map (sibling of qc_n_swap_amp: excite the pair control, then N rounds of fixed-amp swap + an off-resonant AC-Stark tone at the swept amplitude, joint readout -> a (stark_amp, swap_count) transfer map; new 'stark' SquarePulse op, off-resonance via update_frequency + stark_detuning_hz, only the tone AMPLITUDE swept). REQUIRES scqat >= v0.16.0 — the estimator (scqat.estimators.qc_n_stark_amp) is lazy-imported inside estimate(), so the catalog imports fine under an older scqat but `scqo run qc_n_stark_amp` fails LOUDLY with ImportError at estimate (this ONE experiment only; every other experiment is unaffected). OFFLINE-VALIDATED ONLY — the full offline suites are green across scqo, scqat, scqo-qm and scqo-qblox, but register_stark.py run/calibrate and HARDWARE VALIDATION are STILL OWED; treat qc_n_stark_amp results as unvalidated until then. scqo-qblox = no-change version-track re-tag (tree-identical to v1.0.2; its pyproject stays 1.0.1). scqo-contrib stays v0.12.0 and is NOT in the combo (incompatible since greenfield). SERVER UPGRADE (BLUEFORSAS2): INSTALL section 5 — git fetch --tags && git checkout v2.0.0 in scqo/scqo-qm/scqo-qblox + scqat at v0.16.0, then re-run the editable installs per venv (.venv-qm, .venv-qblox, .venv-view; these uv-managed venvs have NEITHER pip NOR uv inside them, so drive it from outside: uv pip install --python <venv>\\Scripts\\python.exe -e D:\\github\\SCQO -e D:\\github\\scqat --no-deps), restart the viewer, then scqo doctor on a student account. Because the history restructure is breaking, upgrade ALL state-writing machines in one pass."

[v1-0-2]
date = 2026-08-10
scqo = "v1.0.2"                  # campaign statistics.png: one figure slot per occurrence for duplicate-step plans
scqo-qm = "v1.0.2"               # no code change; combo version-track (v1.0.1-equivalent tree — qm-session-hardening still deferred)
scqo-qblox = "v1.0.2"            # no code change; combo version-track
scqo-contrib = "v0.12.0"         # still NOT in the combo
scqat = "v0.15.0"                # unchanged pin
notes = "FIX (scqo) — campaign statistics.png drew duplicate-step plans wrong. A plan listing the SAME experiment at several step positions of one repeat (e.g. a 3xT1 stability bundle) had later occurrences OVERWRITE earlier ones in the figure's series, because collect() indexed each child run by repeat_idx ALONE — so the figure plotted only the LAST occurrence per repeat and its n/mean/std/slope annotations DISAGREED with the manifest statistics that `scqo campaign --show` prints (aggregate() had always slotted every (repeat, step) occurrence in step order). collect() now gives one figure slot per occurrence (index = repeat_idx * n_occurrences + step-position rank, ranked against the step positions the experiment occupies campaign-wide), matching aggregate()'s walk, with aligned None gaps for runs missing from a failed or partial repeat. FIGURES ONLY — campaign.json statistics, the writeback aggregates and every run folder were always correct and are unchanged; affected campaigns re-render correctly with `scqo campaign --show <id> --plot`. No upgrade actions. scqo-qm and scqo-qblox are version-track re-tags (tree-identical to v1.0.1); scqo-qm stays at its v1.0.1-equivalent reverted state — the qm-session-hardening session-door change remains deferred pending the QPX1000_4 restart. KNOWN ISSUE FROM v1.0.0/v1.0.1 RESOLVED: the QM reset_method='active' qubit_ramsey hardware failure (deferred here from v1.0.1) was diagnosed 2026-08-10 and active reset was EXONERATED — the cause was the degraded-cluster feedback stall (every feedback program stalls at num_averages/2 shots), an operator cluster-restart matter, not an experiment or code defect; no QM code ships for it in this release."

[v1-0-1]
date = 2026-08-09
scqo = "v1.0.1"                  # no code change; combo version-track
scqo-qm = "v1.0.1"               # xyz-delay coarse pre-wait floored at QUA's 4-cycle minimum
scqo-qblox = "v1.0.1"            # no code change; combo version-track
scqo-contrib = "v0.12.0"         # still NOT in the combo
scqat = "v0.15.0"                # unchanged pin
notes = "FIX (scqo-qm) — qubit_xyz_delay: half_scan_ns < 16 baked the coarse pre-wait as wait(<4), legal to the qm client and to generate_qua_script but refused ONLY by the GATEWAY compiler at run time; now floored at QUA's 4-cycle minimum (timing-safe: the pre-wait positions the segment start, the XY-Z relative axis rides inside the baked segments) with an offline script-level wait-floor pin. No upgrade actions; SCQO and scqo-qblox tags are version-track only. KNOWN ISSUE RE-TARGET: the QM reset_method='active' qubit_ramsey hardware failure recorded at v1.0.0 moves to v1.0.2."

[v1-0-0]
date = 2026-08-09
scqo = "v1.0.0"                  # THE deliberate 1.0: v1 driver restructure + campaign writeback + run --preview
scqo-qm = "v1.0.0"               # RENAMED from LCHQMDriver: package scqo_qm, fused experiments, LCH shells retired
scqo-qblox = "v1.0.0"            # RENAMED from LCHQBDriver: package scqo_qblox
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged, incompatible since greenfield)
scqat = "v0.15.0"                # tomography noise_mode + FFT-seeded benchmarking fit — REQUIRED, see notes
notes = "THE DELIBERATE v1.0.0 — full SemVer applies from here (RELEASING.md). BREAKING RESTRUCTURE, the headline: LCHQMDriver IS NOW scqo-qm (package customized -> scqo_qm, driver-symmetric layout scqo_backend.py + backend/ + experiments/ with ONE FUSED FILE PER EXPERIMENT; scqo a hard dependency; the 22 qualibrate LCH shells + node packages + 8 shell-only probes DELETED — official qualibrate nodes unchanged, calibrations/exclude/ + customized/ frozen archive) and LCHQBDriver IS NOW scqo-qblox (package lchqb -> scqo_qblox; _amp -> _amp_limits; element CLASS names unchanged). Dists scqo-qm / scqo-qblox; GitHub redirects old URLs. Proven by a golden harness: 35/35 QUA programs byte-identical to the pre-restructure baseline; one deliberate contract edit — the tracker/tomography 3-tuple's third element is the acquire CALLABLE. UPGRADE REQUIRES, per machine, in order: (1) git remote set-url + folder rename; (2) per venv UNINSTALL the old dist THEN editable-reinstall (uv pip uninstall --python <venv>\\Scripts\\python.exe superconducting-calibrations | lchqb-driver; uv pip install --python <venv>\\Scripts\\python.exe -e D:\\github\\scqo-qm | scqo-qblox --no-deps — entry points register at INSTALL time; a stale dist leaves a broken duplicate 'qm'/'qblox'); (3) run scqo-qm\\scripts\\migrate_state_scqo_qm.py over EVERY QM backend_config + state_lib folder (customized.* -> scqo_qm.* __class__ paths; verifies POSITIVELY — QUAM otherwise falls back to base classes SILENTLY); (4) re-point ~/.qualibrate config.toml AND config_new.toml ([quam] state_path, [qualibrate.calibration_library] folder, config_new [storage] location) to scqo-qm paths; (5) LCH GUI shells are gone — run experiments via scqo run; the qualibrate GUI serves official nodes only. REQUIRED: scqat >= v0.15.0 (an older scqat feeds a noise_mode spectator's all-zero I/Q into discriminate_states — SILENT garbage). KNOWN ISSUE (target v1.0.1): QM reset_method='active' qubit_ramsey fails on hardware (5Q4C 2026-08-09); thermal default unaffected; QM active reset remains hardware-unvalidated as shipped in v0.24.0. NEW — campaign writeback: finalize replays statistics through each step's update() into PENDING campaign-level suggestions on campaign.json (plan [writeback]: stat mean|median, min_n floor; update='apply' campaigns generate nothing); scqo accept --campaign <id> (same era/staleness guards); ChangeRecord.campaign_id -> provenance status 'campaign'; scqo campaign --suggest <id> regenerates for pre-feature campaigns; viewer /campaigns + /campaign/<id>. NEW — scqo run <name> --preview [--out DIR] [--no-open] [--simulate-ns NS] [--no-simulate]: builds and stops after probe(); Qblox interactive pulse_diagram + timing_table (refused by name above 256 rendered shots); QM qua_script.py + AUTO gateway-simulated waveforms (2 s TCP probe, degrades to script + warning; self-acquiring shells refuse by name); simulated backend refuses; nothing runs, nothing saved. FIX — Qblox acquisition timeout scales per experiment (fixed 300 s killed long FPGA-averaged sweeps; InstrumentCoordinator.timeout mirrored; no user action). FIX — QM drag calibration was 100% dead in v0.25.0 (stale lock_x90 TypeError) and the x90 path died on live states (reference guard now uses get_raw_value); target_gate is Literal['x180','x90'] — BEHAVIOR CHANGE: 'X90' is refused at validation instead of silently writing the x180 knob. FIX — known_devices skips dot-/underscore-prefixed scratch dirs. FIX — scqo-qm scripts/check_real_config.py works again on the roster-threaded backend surface (predated QMDeviceModel's roster argument, crashed at step 2/5; roster_toml_for now ships as scqo_qm.backend.roster_gen, shared with the test fixtures, and a subprocess test pins the PASS line). BEHAVIOR CHANGE — viewer's bare /trends picker + Trends nav removed (device-less /trends -> /device overview; /trends?device=... unchanged). ADDITIVE — qubit_tomography noise_mode spectators (play target gates, dummy-zero record, success=0) + identity-gate wait fix + phase-aligned multiplexing (BEHAVIOR CHANGE: identity target_gate now advances time by the x180 length per rep, was zero-time); qubit_deterministic_benchmarking FFT-seeded multi-start fit. Hardware status: the v1 stack is smoke-validated on 5Q4C 2026-08-09 (resonator_spectroscopy via scqo run + the operator's common QM set); still open on hardware: drag/x90 path, long-Qblox-timeout runs, tomography noise_mode. SERVER UPGRADE (BLUEFORSAS2): INSTALL section 5 with the restructure steps above; old clone URLs keep working via redirects. After checkout, re-run the editable installs per venv as in step (2) plus -e D:\\github\\SCQO -e D:\\github\\scqat."

[v0-25-0]
date = 2026-08-08
scqo = "v0.25.0"                 # readout-schema cutover + qc_n_swap_amp + parity-switch split + t1 tracking + fit_tau_seeds
lchqbdriver = "v0.25.0"          # acq_rotation handedness negation + discrete-parity probe + schema-decode cutover
lchqmdriver = "v0.25.0"          # t1-tracking probes + discrete-parity probe + schema reductions + apply_distortion CLI
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged since v0.13.0)
scqat = "v0.14.0"                # joint_population estimators + parity split + t1 estimators + cryoscope seeding — REQUIRED, see notes
notes = "REQUIRED: scqat MUST be at v0.14.0 — three floors, two of them SILENT: (1) the readout-schema cutover is LOUD (SCQO's pair estimate() requires scqat's joint_population estimators and both drivers import SCQO's joint_state_labels/states_to_joint_population — an under-version stack fails with ImportError/ContractError); (2) fit_tau_seeds is SILENT (an older scqat IGNORES unknown estimator kwargs, so --set fit_tau_seeds=[...] would be a no-op, not an error); (3) the discrete parity fit keys (p_intercycle_flip, p_m1_high, p_m2_high) are read with .get(..., nan), so an older scqat degrades SILENTLY to NaN. Re-run the editable installs (line at the end). BREAKING — THE READOUT SCHEMA IS NOW DECLARED, NOT INFERRED (all four repos move in LOCKSTEP; canonical text TUTORIAL section 11): 'state' is ALWAYS a per-shot integer level (qutrit-capable, never a probability), 'population' the averaged marginal, 'joint_population' a multi-qubit target's joint distribution (marginals = partial trace, derived not stored). Pre-cutover pair-run datasets (p_high/p_low/p_ee) can NO LONGER be re-analysed — the reconstruction was deleted; run folders stay immutable and keep their old artifacts. NEW EXPERIMENT riding the cutover — qc_n_swap_amp, the record-only swap error-amplification map (both readout modes; partial-swap workflow = TUTORIAL section 12: coupler flux is the angle knob, N-period = pi/theta). QM hardware-validated (5Q4C q1_q2, 2026-08-06); Qblox decode offline-only. BREAKING RENAME — qubit_parity_switch IS NOW qubit_parity_switch_continuous (NO alias, house rule: one clean cutover): re-key any parameters.toml [qubit_parity_switch] table to the new name or its defaults SILENTLY stop applying; scripts and campaign plans fail by name; scqat artifacts follow (parity_switch_* -> parity_switch_continuous_*); saved run folders are immutable and keep the old experiment name — query historical runs by the OLD name. NEW EXPERIMENT — qubit_parity_switch_discrete on BOTH backends: per cycle M1 - depletion - x90 - idle - y90 - M2 - pad at a fixed cycle_period_ns; parity = m1 XOR m2 WITHIN each cycle, so M1's re-projection makes slow sampling T1-safe (two acquisition bins per cycle on Qblox; max_num_shots default 1.4e6; p_intercycle_flip is the QND-chain health check). Discrete has NO idle_multiple (continuous keeps it) — extend the period via cycle_period_ns only. Pipeline hardware-proven on chipA/Qblox 2026-08-08; PHYSICS VALIDATION STILL PENDING on both backends (chipA currently sick). BEHAVIOR CHANGE — resonator_spectroscopy depletion_factor default 5 -> 10 (tau_d ~ 10/kappa): the PROPOSED readout_depletion_s doubles on the next accepted run; standing knobs untouched until then. UPGRADE REQUIRES (Qblox) — EVERY Qblox dut_config's persisted acq_rotation is the MIRROR of the calibrated angle: the view now NEGATES readout_rotation_rad at the vendor boundary (Qblox acq_rotation is OPPOSITE-HANDED to QM's integration-weights angle; the old push mirrored the frame — chipA 2026-08-08 discriminated runs classified every shot |g>). Re-run single_shot_readout + accept (or `scqo set <q>_ro.readout_rotation_rad=<current value>` to re-push through the fixed boundary) on every Qblox device config BEFORE any use_state_discrimination or active-reset run; chipA re-validation pending (chip sick). NEW EXPERIMENTS — qubit_t1_ade + qubit_t1_bayesian, T1-vs-LAB-TIME tracking (record-only — qubit_relaxation stays the t1_s authority; QM-only probes; both join the QM active-reset carrier census 4 -> 6): ADE = 3-delay Analytical Decay Estimation with on-FPGA closed form + analytic sigma (arXiv:2602.11912, SPAM cancels, host bootstrap cross-check); Bayesian = per-shot adaptive Bayes (arXiv:2506.09576, u = 1/k so k grows past fixed-point range, tau = c*T1_est, interleaved classical-decay validation; needs discriminator + confusion matrix). BOTH HARDWARE-VALIDATED on 5Q4C q1 2026-08-08 (ADE t1 = 29.95 us; Bayes 33.25 us with adaptive k reaching 17.5 and classical validation 28.2 us — all against qubit_relaxation's 30.4 us). NEW (cryoscope fitting trio, additive): fit_tau_seeds on BOTH cryoscopes — explicit prior-knowledge tau seeds in seconds from the CLI (--set fit_tau_seeds=[1e-7,2.3e-9]); the seeded joint fit RACES the automatic path and the better residual wins (metadata seed_method='seeded'); axis-agnostic, so it is the first seeding mechanism available on the spectroscopy cryoscope's log wait grid. Hankel-SVD Matrix Pencil seeding for the ramsey cryoscope — RamseyCryoscopeEstimator runs BOTH the MPM-seeded and fractions fits and keeps the better residual (provenance seed_method/mpm_n_modes/mpm_oscillatory — the oscillatory flag is the consider-FIR signal); BENIGN BEHAVIOR CHANGE: default ramsey fits may now resolve components the old sequential fit merged; also fixes a hankel.py crash (growing noise-pole overflow) affecting any hankel_decompose consumer. fit_step_response degeneracy guard — bounded amplitudes (amp_max=2.0), identifiability tau floor (>= half the first sample time), cancelling close-tau pair collapse: three real degenerate fits (+-22866, +-22, +-77.8 amplitude pairs) become honest single taps or clean failures; healthy fits are bit-identical; both cryoscope estimators inherit via defaults. NEW TOOLING (QM) — python -m customized.scqo.apply_distortion --target qN [--run <run_id>] [--extend] [--clear] [--dry-run]: one-command cryoscope-tap apply into the OPX z-output exponential filter (offline device/setup resolve, taps from a named run's fit or the accepted facts, --clear = fresh-line reset); hardware-validated 2026-08-06 (full clear -> measure -> apply -> verify loop, ns window flat to noise). Facts stay record-only — applying remains explicit/opt-in. PROCESS — the 0.x version rule is codified in RELEASING.md (bump = max fragment severity: breaking/additive -> y+1, all-fix -> z+1; full SemVer at a deliberate v1.0.0) and the fragment kind vocabulary gains 'fix'. Re-run `uv pip install -e . --no-deps` in all three venvs (.venv-qm, .venv-qblox, .venv-view) plus the scqat checkout; these uv-managed venvs have NEITHER pip NOR uv inside them, so drive it from outside: `uv pip install --python <venv>\\Scripts\\python.exe -e D:\\github\\SCQO -e D:\\github\\scqat --no-deps`."

[v0-24-0]
date = 2026-08-05
scqo = "v0.24.0"                 # ramsey/spectroscopy cryoscope split + viewer decouple + error cause chain
lchqbdriver = "v0.24.0"          # Qblox flux-distortion config wrapper + glue-test pin
lchqmdriver = "v0.24.0"          # QM active reset + spectroscopy_cryoscope probe + OPX z-filter writer + parity progress fix
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged since v0.13.0)
scqat = "v0.13.0"                # cryoscope estimator rename/new + Ramsey collapse fix — REQUIRED, see notes
notes = "REQUIRED: scqat MUST be at v0.13.0 — the cryoscope estimator RENAMED (scqat.estimators.cryoscope -> ramsey_cryoscope) and the NEW scqat.estimators.spectroscopy_cryoscope exist only at this tag, so an under-version scqat fails qubit_ramsey_cryoscope / qubit_spectroscopy_cryoscope runs LOUDLY with ImportError. Re-run the editable installs (line at the end). BREAKING RENAME — qubit_cryoscope IS NOW qubit_ramsey_cryoscope (the family gained a second member, so the name must say which mechanism). NO alias or shim ships (house rule: one clean cutover): scripts, campaign plans and parameters.toml tables saying qubit_cryoscope now fail by name; edit the name and re-run. Saved run folders are immutable and keep the old experiment name — query historical runs by the OLD name. NEW EXPERIMENT — qubit_spectroscopy_cryoscope (QM-only probe), the LONG-TIME flux-distortion sibling: qubit spectroscopy DURING a long weak flux pulse (settable drive length, asymmetric detuning window) reads the slow tail that ramsey_cryoscope's short window cannot see; same paired distortion_amp/distortion_tau_s facts with REPLACE semantics, and the two experiments propose INDEPENDENTLY. v1 is BAND-LIMITED (no LO shift — the detuning window must fit the current LO band). NEW — the cryoscope taps now REACH the instrument: LCHQMDriver's apply_exponential_filter writes accepted distortion taps into the OPX z-output exponential filter (a documented MANUAL step in v0.23.0), LCHQBDriver gains the matching Qblox flux-distortion config-value wrapper, and scqat gains the sum <-> single-pole-cascade predistortion converter. NEW — ACTIVE RESET ON QM: reset_method='active' is now realized on the QM backend for the four coherent-drive carriers (qubit_relaxation, qubit_ramsey, qubit_echo, qubit_power_rabi) through the one door customized/scqo/experiments/_reset.py (check_reset_method + reset_max_attempts, re-checked in QMBackend.acquire before probe()); every other experiment still REFUSES it by name. QM rules, each refused loudly: active_reset_rounds maps to QUAM max_attempts as an UPPER bound (not Qblox's exactly-N); BOTH readout_threshold AND readout_rus_threshold must be governed (uncalibrated is None on QM and would die nameless deep in QUA); readout_depletion_s must be governed (the 16 ns factory default is refused); thermalization_time_ns together with 'active' is refused, not ignored. WARNING — NOT YET HARDWARE-VALIDATED: offline tests prove the policy and that the QUA program builds, but the feedback loop is hardware, and the chipA walkthrough in _reset.py's docstring is STILL OWED (shipped by decision 2026-08-05). Treat QM active-reset results as unvalidated until the walkthrough passes (refusals fire, thermal-vs-active T1 and T2*/detuning consistent), and note a personal parameters.toml [qubit_ramsey] reset_method='active' line is now LIVE on QM. FIX (QM), AND IT UNBLOCKS A WHOLE EXPERIMENT — qubit_parity_switch on QM crashed AFTER the full QUA program ran: QMBackend.acquire took the progress-counter total from getattr(params, 'num_shots', 1), which returns the present-but-None value for record_time_s-derived experiments, and the vendor progress_counter died on int/None. The total now resolves through experiment.resolved_num_shots() (the same number the probe buffers with) — hardware-verified on chipA (run completes, dataset.nc written). This fix rode in LCHQMDriver commit 2377bc5, whose message names only active reset. FIX (scqat), AND IT CAN FLIP A RUN'S OUTCOME — the Ramsey time-domain fit collapse: the phi=0 phase seed plus the a>=0 amplitude bound collapsed anti-phase/beat fringes to the flat mean (a ~ 1e-11) while still reporting SUCCESSFUL, and force_model='beat' collapsed identically. Both fitters now seed amplitude+phase from an LS quadrature projection (new tools helper seed_amp_phase) and ramsey_fit folds a MIN_VAR_EXPLAINED=0.05 gate into success (new var_explained result field): a collapsed fit now reports success=False and the run marks FAILED. Auto-selection now picks the beat model on real charge-parity data (both chipA reference runs: var_explained 0.93/0.95, no force_model needed). NO re-measurement — re-analysing an old run reproduces the corrected fit; existing run folders keep their old collapsed figures (immutable). VIEWER — decoupled from the launching account: bare /device is an all-samples overview (no cfg.device dependence), so the BLUEFORSAS2 server stops needing ?device= once upgraded to this combo. RUN RECORDS — the error field now preserves exception CAUSE CHAINS: a bare 'raise Exception from e' (qualang_tools' qm_session wrapping a QM open failure) no longer collapses to 'Exception: ' but carries the cause (e.g. DEADLINE_EXCEEDED), and an empty-message exception records its type name; anything parsing that field sees richer text. COSMETIC (scqat) — psd_model='constrained' parity PSD annotation names the FITTED parameter first (Gamma, then f_c = Gamma/pi; 'independent' unchanged; numbers unchanged), and figures ALWAYS render even on a failed fit (a failed run keeps its raw-data PNGs instead of losing all figures to one plotter crash). TEST-ONLY, no runtime change — the subprocess CLI harnesses (SCQO test_cli_run/test_cli_cooldown, both drivers' test_scqo_glue) now pin an empty parameters_file so the runner's real ~/.scqo/parameters.toml cannot flip a sim fit to failed. NOT IN THIS COMBO: qc_n_swap_amp (experiment + estimator) is stashed in SCQO and scqat, deferred to a later cut. Re-run `uv pip install -e . --no-deps` in all three venvs (.venv-qm, .venv-qblox, .venv-view) plus the scqat checkout; these uv-managed venvs have NEITHER pip NOR uv inside them, so drive it from outside: `uv pip install --python <venv>\\Scripts\\python.exe -e D:\\github\\SCQO -e D:\\github\\scqat --no-deps`."

[v0-23-0]
date = 2026-08-02
scqo = "v0.23.0"                 # cryoscope + xyz_delay + parity psd_model + pair_swap maps
lchqbdriver = "v0.23.0"          # xyz_delay flux_delay_s Unrealized (Qblox has no probe yet)
lchqmdriver = "v0.23.0"          # QM cryoscope + xyz_delay probes
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged since v0.13.0)
scqat = "v0.12.0"                # cryoscope + xyz_delay + pair_swap estimators — REQUIRED, see notes
notes = "REQUIRED: scqat MUST be at v0.12.0. Three new SCQO experiments lazy-import scqat estimators that DID NOT EXIST before this tag (qubit_cryoscope -> scqat.estimators.cryoscope + tools.step_response_fit; qubit_xyz_delay -> scqat.estimators.xyz_delay + tools.fit_triangle; pair_swap_chevron/pair_swap_flux_map -> scqat.estimators.pair_swap_{chevron,flux_map}), so an under-version scqat fails the run LOUDLY with ImportError, not a silent NaN. Re-run `uv pip install -e . --no-deps` in all three venvs (.venv-qm, .venv-qblox, .venv-view) plus the scqat checkout; these uv-managed venvs have NEITHER pip NOR uv inside them, so drive it from outside: `uv pip install --python <venv>\\Scripts\\python.exe -e D:\\github\\SCQO -e D:\\github\\scqat --no-deps`. NEW EXPERIMENT — qubit_cryoscope (QM-ONLY), the flux-line step-response calibration. A Ramsey phase-tomography sequence (x90 - swept flux-pulse DURATION - fixed idle - swept closing-pulse FRAME - x90) reconstructs the flux line's transient: lock-in phase per duration -> savgol derivative -> detuning -> sqrt arch -> tail-normalize -> multi-exponential fit -> predistortion taps (amp, tau). update() proposes the flux channel's paired FACTS distortion_amp (dimensionless = amp/a_dc) + distortion_tau_s (seconds), REPLACE semantics -- new catalog FieldSpecs on the flux channel kind (fact, non-portable, paired). Facts go to physical.json and are NEVER pushed: LOADING the taps into the instrument output filter (OPX exponential_filter) is a MANUAL vendor step, not automated. Single-target only; needs a calibrated x90; the duration axis must be every ns 1..max. On Qblox the base probe() raises NotImplementedError (no cryoscope probe yet). NEW EXPERIMENT — qubit_xyz_delay (QM-ONLY probe today), the XY<->Z line-delay calibration. Slides a fixed X180 and a same-length Z (flux) pulse past each other at 1 ns resolution over two preps (|e>/|g>) and fits the |e>-|g> contrast to the TRIANGLE overlap of the two pulses; the peak is the flux line's output delay vs the drive line, written to the NEW flux-channel knob flux_delay_s so simultaneous XY+Z gates coincide. Realized on QM PORT-LEVEL (q.z.opx_output.delay, integer ns, negatives REFUSED) and ABSOLUTE (old + fitted, unlike the vendored 16a node's +=). On Qblox flux_delay_s is UNREALIZED: the home exists (hardware_options.latency_corrections) but no Qblox probe writes it, so the knob is unsettable and invisible in pull mode until a Qblox XY-Z probe lands. Additive knob -> every driver must bind-or-decline it (the Qblox fieldmap drift alarm enforces this). Needs a calibrated x180 (its length sets the triangle width) and half_scan_ns > pi length. PARITY CHANGE, AND IT FLIPS A DEFAULT -- qubit_parity_switch gains psd_model: 'constrained' (NEW DEFAULT) vs 'independent'. Both fit the same Welch PSD Lorentzian+floor; 'constrained' is the reference RTS model F^2*4G/((2G)^2+(2pi f)^2)+(1-F^2)*dt with a SINGLE shared mapping fidelity F and dt FIXED to the shot period (a 2-param {F,G} fit) -- F comes out directly. 'independent' is the previous free-floor fit that reads F twice (plateau + floor) with their ratio as the model-consistency diagnostic. BECAUSE THE DEFAULT FLIPPED, a default run now reports ONE mapping_fidelity with mapping_fidelity_floor/ratio = NaN (that pair is now 'independent'-only); the constrained fit's quality is the NEW psd_fit_residual float instead. psd_model is a STRING so it lives in the scqat metadata + run parameters, not the float-only Result.fit. Figure keys unchanged; NO re-measurement needed -- re-analysing a v0.22.0 run under either model reproduces its rate (constrained runs ~0.83x the independent on the two chipA references, both healthy). NEW ANALYSIS — pair_swap_chevron and pair_swap_flux_map, previously record-only with no artifacts, now draw scqat joint-state-population maps (P00/P01/P10/P11 reconstructed from the probe's lossless role-marginal encoding) as figure+plotdata+metadata per pair. Still RECORD-ONLY for the device: the SUCCESS verdict stays in the experiment, the estimator proposes nothing. Additive: no device action, no data action. Run folders are immutable throughout -- no re-measurement anywhere in this release."

[v0-22-0]
date = 2026-08-01
scqo = "v0.22.0"                 # idle_multiple + the mapping fidelity on the run record
lchqbdriver = "v0.22.0"          # the 5-minute acquisition timeout
lchqmdriver = "v0.22.0"          # no-change re-tag (nothing in the QM driver moved)
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged since v0.13.0)
scqat = "v0.11.0"                # mapping fidelity + the merged debug panel — REQUIRED, see the notes
notes = "REQUIRED: scqat MUST be at v0.11.0, and unlike v0.19.0 this one DEGRADES SILENTLY below it — nothing raises. SCQO reads the new keys with .get(..., nan), so an under-version scqat yields a run record whose mapping_fidelity* fields are all NaN and figures under the OLD names, with no error anywhere. Re-run the editable installs (line at the end) and check `scqo --version` reports 0.22.0. NEW — THE MAPPING FIDELITY F, FOR FREE. The Lorentzian scqat already fits, A/(1+(f/f_c)^2)+B, IS the reference RTS spectrum 4F^2*G/((2G)^2+(2pi f)^2) + (1-F^2)*dt written in different variables: our series is 0/1 (a quarter of the variance) and welch returns the one-sided density (twice), so S_ours = S_P/2 and f_c = G/pi, A = F^2/(2G), B = (1-F^2)*dt/2. No refit was needed — A and B were ALREADY that model's 4F^2 and (1-F^2)dt terms carried as free parameters — so the sequence mapping fidelity F (= 1 - 2*eps for a per-sample mapping-error probability eps) falls out twice: mapping_fidelity = sqrt(2*pi*f_c*A) from the plateau and mapping_fidelity_floor = sqrt(1 - 2*B/dt) from the floor, plus mapping_fidelity_ratio. The normalization is self-consistent by construction (Lorentzian integrates to F^2/4, floor to (1-F^2)/4, summing to the 1/4 variance of a balanced 0/1 series) and a power-budget test pins it. THE TWO ESTIMATES ARE NOT REDUNDANT AND THE RATIO IS THE POINT: on a DIRECTLY sampled telegraph with genuinely white mapping error the floor estimate is exact (measured 1.000x of truth across G = 2-200 Hz, eps = 0-5 %) while the plateau runs ~2 % low from Welch leakage — but on the XOR-derived parity of this no-reset sequence the induced noise is lag-1 correlated, and then the FLOOR SATURATES AT 1, reporting a flawless sequence, while the plateau collapses (planting G = 20 Hz at dt = 50 us: at 0.2 % readout error the corner inflates 71x while F_floor still reads 1.000 and F_plateau drops to 0.285). So the plateau is the headline and a ratio well below 1 means the Lorentzian-plus-WHITE-floor model does not describe the data — which is the readable symptom of the correlated readout error that also inflates the rate. CRITICALLY, psd_contrast IS BLIND TO THIS: those corrupted fits keep a large A/B. It is REPORTED, NEVER GATED — there is no calibrated threshold yet, and a wrong one would reject good runs, exactly how the old p_switch ceiling nearly refused a textbook fit. Real chipA run 20260801-191415-048 scores F = 0.578 (plateau) / 0.634 (floor), ratio 0.91, i.e. a consistent fit at ~20 % per-sample mapping error. FIGURE KEYS CHANGED — SCRIPTS THAT GLOB THE PNG NAMES MUST BE UPDATED: parity_switch `trace` and `parity` are MERGED into one shared-x DEBUG panel `timetrace` (readout over parity, first 100 points), so parity_switch_trace.png and parity_switch_parity.png are replaced by parity_switch_timetrace.png; `psd` and `state_psd` are unchanged. The two series are one differencing step apart, which is the whole reason to see them together, and at ~30 us per shot a 100-point window is ~3 ms — far under one switching period, so the parity row looking CONSTANT is correct, not a fault. The parity tick labels are now XOR=0 / XOR=1, not even / odd: this measurement fixes the parity only up to a global label, so which value is the even charge parity is unknowable from it. The readout panel draws the mean of the WHOLE trace (new key p_state_high — a third fraction, distinct from p_parity_odd, the parity's level, and p_switch, how often the parity changes). NEW — idle_multiple: the derived idle becomes N / (2 x parity_delta_f_hz). The gain is NOT a lower f_min at fixed record time (unchanged) — it is that long runs are BIN-limited: at chipA's 30.4 us shot the Qblox 3e6-bin ceiling caps a run at 91 s (f_min 0.088 Hz), while N=3 stretches the shot to ~48.5 us so the same bins buy 145 s (f_min 0.055 Hz). It also suppresses the readout-error bias (1 + 2*eps/(G*dt)). The cost is T2*: contrast decays as exp(-t/T2*), and t2_star_s is a mode FACT, unreachable at define_sweep time, so that stays a documented caution rather than an automatic check. ONLY ODD N CARRIES SIGNAL — the branches sit at +-delta_f/2, so the parity phase over N/(2*delta_f) is +-N*pi/2 and the readout goes as sin(+-N*pi/2): +-1 for odd N, exactly 0 for even N (both parities land on the same pole). Even N is ALLOWED but warns at define_sweep naming the reason and the nearest odd values; it fails safe either way, since a zero-contrast run produces no telegraph and MIN_PSD_CONTRAST refuses the fit. max_derived_idle_ns applies to the FINAL idle (base x N) and the refusal distinguishes the two causes. EXPECT TO HIT IT: N=3 on chipA is 27.1 us against the 20 us default ceiling, so raise max_derived_idle_ns when using N>1. An explicit idle_time_ns still wins outright and is NOT multiplied. QBLOX FIX — the acquisition timeout was 120 s and killed a 30 s-record parity run on 2026-08-01 (run 20260801-215523-624, 'Sequencers slot 4 sequencer 0 did not stop in timeout period of 2 minutes'); it is now 300 s. qubit_parity_switch is the binding case because it is parameterized by RECORD TIME and plays one long uninterrupted shot train, and the 3e6-bin ceiling bounds that at 91-145 s — so 120 s sat right inside the legal band. NOTE THE UNIT TRAP for anyone tuning it: HardwareAgent.run(timeout=) is documented in SECONDS but the instrument coordinator hands the cluster timeout_sec // 60 MINUTES (floor division), so the real granularity is one minute and anything under 60 s truncates to a ZERO-minute deadline — keep it a multiple of 60. NO RE-MEASUREMENT NEEDED for v0.21.0 data: re-analysing that chipA run through v0.22.0 reproduces 2.3525 Hz and corner 0.748823 Hz exactly and adds the F numbers. Re-run `uv pip install -e . --no-deps` in all three venvs (.venv-qm, .venv-qblox, .venv-view) plus the scqat checkout; these uv-managed venvs have NEITHER pip NOR uv inside them, so drive it from outside: `uv pip install --python <venv>\\Scripts\\python.exe -e D:\\github\\SCQO -e D:\\github\\scqat --no-deps`."

[v0-21-0]
date = 2026-08-01
scqo = "v0.21.0"                 # qubit_parity_switch is parameterized by RECORD TIME
lchqbdriver = "v0.21.0"          # shot count from the neutral layer
lchqmdriver = "v0.21.0"          # shot count from the neutral layer
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged since v0.13.0)
scqat = "v0.10.0"                # spectral gate + the reach diagnostics — REQUIRED
notes = "PARAMETER CHANGE: qubit_parity_switch now takes record_time_s (default 30 s), not num_shots. What limits the measurement is the parity spectrum's LOWEST FREQUENCY BIN, 8 / record_time_s, so a rate whose corner (rate / pi) sits near it is fitted from only a handful of points. The first real chipA run (20260801-191415-048) made this concrete: 30.379 s of record put the low edge at 0.2633 Hz against a corner at 0.7488 Hz — only 2.84x of headroom, with barely three bins on the plateau. The shot count is DERIVED using the target's estimated shot period (idle + readout + depletion + the two pi/2 pulses). NOTE THE DIVISOR IS THE SHOT PERIOD, NOT THE IDLE: that run has a 9.04 us idle inside a 30.379 us shot, so dividing by 1/(2 x parity_delta_f_hz) would under-record by 3.4x. num_shots SURVIVES as an explicit override and BYPASSES max_num_shots (default 2e6, under the Qblox 3e6 acquisition-bin limit so the neutral refusal fires first) — exactly as idle_time_ns bypasses max_derived_idle_ns; the ceiling guards the derived value only. Old scripts passing num_shots explicitly keep working unchanged. With several targets the count is the MAX over them, so every target records at LEAST record_time_s. The achieved record_time_s (from the period the probe really scheduled) is recorded beside the request, since the count came from an estimate. GATE CHANGE, AND IT CAN FLIP A RUN'S OUTCOME: MAX_ODD_FRACTION on p_switch is REPLACED by MIN_PSD_CONTRAST = 3.0 on the plateau-to-floor ratio A/B. p_switch was the wrong quantity — any shot-to-shot noise drives it toward 0.5 whether or not a knee exists, and that chipA run reported p_switch = 0.294 against the old 0.40 ceiling despite a textbook fit (A/B = 7826), i.e. within 1.4x of refusing a good measurement. Contrast separates by eight orders of magnitude: ~1e-9 on uncorrelated data against 7e2-8e6 on real telegraphs, including one buried under 30% spurious flips. A run previously refused for p_switch > 0.4 may now legitimately PASS; p_switch is still reported but no longer decides. NEW DIAGNOSTICS on the run record: psd_freq_min_hz (the low edge — what your record length bought), psd_contrast, corner_margin_low (= corner / low edge). corner_margin_low is REPORTED, NEVER GATED — a thin margin means a longer record would tighten the fit, which is your decision, and refusing would throw away a usable measurement. The PSD figure draws the low edge and says so. NO RE-MEASUREMENT NEEDED for v0.20.0 data: re-analysing that chipA run through v0.21.0 reproduces 2.3525 Hz and corner 0.748823 Hz exactly, and now passes on contrast. Re-run `uv pip install -e . --no-deps` in all three venvs (.venv-qm, .venv-qblox, .venv-view) plus the scqat checkout; these uv-managed venvs have NEITHER pip NOR uv inside them, so drive it from outside: `uv pip install --python <venv>\\Scripts\\python.exe -e D:\\github\\SCQO -e D:\\github\\scqat --no-deps`."

[v0-20-0]
date = 2026-08-01
scqo = "v0.20.0"                 # the parity is the pair difference, not the readout
lchqbdriver = "v0.20.0"          # docs only (the probe is unchanged and correct)
lchqmdriver = "v0.20.0"          # docs only (the probe is unchanged and correct)
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged since v0.13.0)
scqat = "v0.9.0"                 # the fix itself — REQUIRED, see the notes
notes = "RETRACTION: EVERY parity_rate_hz FROM v0.19.0 IS INVALID. qubit_parity_switch fitted its Lorentzian to the wrong time series. The sequence is a unitary, so it maps antipodal Bloch vectors to antipodal ones and |0> and |1> can NEVER give the same outcome; with no qubit reset and a QND readout each shot starts where the last was projected, so s[i] = s[i-1] XOR parity[i]. The readout is the RUNNING XOR of the parity, and the CONSECUTIVE-PAIR DIFFERENCE is the parity telegraph. v0.19.0 fitted the readout, i.e. an integrated telegraph, and its rates mean nothing. Verified: fitting the pair series recovers 50.53 Hz against a 50 Hz truth while fitting the readout fails outright. ACTION: DELETE any parity_rate_hz accepted into physical.json from a v0.19.0 run — do not merely re-check it. THE RAW DATA IS FINE: only the analysis was wrong, so re-analysing an existing run folder is enough and no re-measurement is needed. ROOT CAUSE, now corrected in SCQO and both drivers: the design docs claimed 'the sequence re-prepares the equator each shot regardless of the starting pole'. It re-prepares at the OPPOSITE point. The absence of a qubit reset is consequently REQUIRED and not an optimization — a reset would sever the XOR chain that carries the parity. RENAMES (contract change, hence the minor bump): scqat's p_excited -> p_high and p_odd -> p_switch; scqo's fit dict now carries p_switch, p_parity_odd and n_parity_switches. NOTE p_parity_odd ~ 0.5 is HEALTHY — it only says the chip sits in each parity about half the time — while p_switch is the parity's own switching fraction and the quantity MAX_ODD_FRACTION = 0.40 guards. Under v0.19.1 (prepared, never published) that guard read the wrong series and would have rejected every healthy run. READOUT FIDELITY NOW MATTERS SHARPLY: one bad shot corrupts TWO adjacent parity samples, so the corruption is lag-1 correlated rather than white and the rate inflates as 1 + 2 x eps / (rate x shot_period) — measured 1.4x at eps = 0.1% and 6.6x at 0.6%. Aim for eps < 0.1 x rate x shot_period, and note that sampling SLOWER suppresses the bias (the opposite of the usual instinct) as long as p_switch stays under 0.4. PLOTS: four panels now — `trace` (raw readout, the running XOR), `parity` (the measured telegraph, and the series that is fitted), `psd` (the parity spectrum WITH the Lorentzian fit) and `state_psd` (the raw readout spectrum, deliberately UNFITTED and diagnostic-only). Both time-domain panels are scatter, since at 1e6 shots a connected step plot fills solid. Re-run `uv pip install -e . --no-deps` in all three venvs (.venv-qm, .venv-qblox, .venv-view) plus the scqat checkout; these uv-managed venvs have NEITHER pip NOR uv inside them, so drive it from outside: `uv pip install --python <venv>\\Scripts\\python.exe -e D:\\github\\SCQO -e D:\\github\\scqat --no-deps`."

[v0-19-0]
date = 2026-08-01
scqo = "v0.19.0"                 # qubit_parity_switch + the Ramsey beat splitting it feeds on
lchqbdriver = "v0.19.0"          # the parity-monitor probe, AND the Ramsey virtual-detuning fix
lchqmdriver = "v0.19.0"          # the parity-monitor probe + shell
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged since v0.13.0); its sandbox experiments still
                                 # predate the v0.17.0 amplitude renames
scqat = "v0.8.0"                 # the telegraph-PSD reduction + the parity_switch estimator — REQUIRED, see the notes
notes = "REQUIRED: scqat MUST be at v0.8.0 — qubit_parity_switch imports scqat.estimators.parity_switch, which does not exist before it, so an under-version scqat fails the run outright (loudly, unlike v0.18.0's silent trap). Re-run `uv pip install -e . --no-deps` in ALL THREE venvs (.venv-qm, .venv-qblox, .venv-view) plus the scqat checkout: an editable install serves live source while reporting a stale version, so `scqo doctor` can read correct while the code is not. NEW — qubit_parity_switch, the charge-parity switching-rate monitor. y90 - fixed idle - x90 - measure, recorded as num_shots single shots with ONLY the resonator depletion wait between them (it carries NO reset_method at all: the sequence re-prepares the equator every shot, and the shot cadence IS the telegraph timebase a reset would stretch). The idle is 1/(2 x parity_delta_f_hz), a +/- pi/2 parity phase, and the 90-degree-shifted second pulse measures sin of it — ODD in parity, where a same-axis pair measures the even cos and sees nothing. The discriminated 0/1 trace is a random telegraph signal; the Lorentzian corner of its Welch PSD gives the rate (parity_rate_hz = pi x corner), a new transmon-family mode FACT. It is fed by qubit_ramsey's beat model, which gains a `ramsey_model` Literal to force the two-frequency fit and now proposes the splitting as the new drive-channel MONITOR parity_delta_f_hz — the splitting has to be a channel monitor because mode facts are unreachable at define_sweep time. Workflow: run qubit_ramsey with ramsey_model='beat' -> scqo accept -> run qubit_parity_switch -> scqo accept; a 2-step campaign repeats it (update='apply' tracks the drifting splitting, update='none' monitors at fixed calibration). It also REQUIRES an accepted single_shot_readout (stored pos_* centres pin the trace discrimination) and an accepted resonator_spectroscopy (readout_depletion_s governs the shot cadence). FIX, AND IT CHANGES ACCEPTED NUMBERS ON QBLOX: the Qblox Ramsey's artificial detuning never reached the instrument. X90(qubit, phase=...) silently DISCARDED the phase (not an Rxy factory kwarg, so it landed in device_overrides and the compiler dropped it) on a schedule that still compiled, so every Qblox Ramsey ever run measured |err| alone — usually under scqat's one-cycle gate, fitted as 'relaxation' and marked FAILED, which is why it went unnoticed. Now Rxy(theta=90, phi=-360*detuning*tau), and the NEGATIVE sign is load-bearing: a positive ramp gives |applied - err| and walks the drive to an absorbing point at err = 2*applied where detuning_error_hz reads exactly 0.0 and the fit looks perfect. A converged Ramsey therefore does NOT prove the sign — cross-witness with qubit_spectroscopy, which finds an absolute peak. Any Qblox qubit frequency calibrated by Ramsey before this release should be re-measured. LCHQMDriver's claim that a frame rotation is 'the opposite handedness to a pulse-axis phase' was FALSE and is corrected; the QM SIGN was always right and does not change."

[v0-18-0]
date = 2026-08-01
scqo = "v0.18.0"                 # the qutrit readout sibling + the thermal-population experiment (n_th's first writer)
lchqbdriver = "v0.18.0"          # the ground-only IQ-cloud probe
lchqmdriver = "v0.18.0"          # GEF and ground-only probes for the scqo readout family
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged since v0.13.0); its sandbox experiments still
                                 # predate the v0.17.0 amplitude renames
scqat = "v0.7.0"                 # pinned centres survive the fit and the plot window — REQUIRED, see the notes
notes = "REQUIRED, AND THE FAILURE IS SILENT: scqat MUST be at v0.7.0. qubit_thermal_population prepares only |g> and pins the readout channel's STORED g/e centres into the mixture fit, i.e. (n_state, n_center) == (1, 2). Before scqat's fix, discriminate unpacked only n_state centres and — in its own commit's words — SILENTLY DROPPED THE REST: on scqat 0.6.0 the fit does not raise, it quietly returns a wrong n_th. There is no version pin protecting you (SCQO declares a bare `scqat` on purpose — the lab and CI both install scqat EDITABLE, which bypasses resolution entirely, so a floor would enforce nothing; this manifest is the authority, which is exactly why it says so here). So: re-run `uv pip install -e . --no-deps` in ALL THREE venvs (.venv-qm, .venv-qblox, .venv-view) plus the scqat checkout — an editable install serves live source while reporting a stale version, so `scqo doctor` can read correct while the code is not. NEW — single_shot_readout_gef, the QUTRIT sibling of single_shot_readout. Same per-shot prepared_state x shot_idx contract with one more prepared state: prepared_state [0,1,2] -> a 3x3 confusion matrix (counted and fitted twins), fidelity_g/e/f, and pos_{g,e,f}_* monitors (three new catalog monitor fields). It proposes NO discriminator knob, deliberately: readout_rotation_rad / readout_threshold describe a scalar cut on ONE rotated quadrature, which separates two blobs and has no three-state analogue — |g>, |e> and |f> sit on a TRIANGLE in the IQ plane and are classified by nearest centre in 2-D. The two-state single_shot_readout remains THE authority for those two knobs; do not 'fix' this by having the qutrit run propose them. QM-ONLY: preparing |f> needs a calibrated 1->2 (EF) pi pulse, which only the QM backend has today — a Qblox session gets the base probe()'s NotImplementedError. Qblox has the <q>.12 clock but no gate targets it. NEW — qubit_thermal_population, the first writer of the mode fact n_th. single_shot_readout already reports this number as a by-product of preparing BOTH states; this experiment prepares only |g> and spends every shot on the ground-state cloud, which is what a 1-5% population needs — the number is a small minority of a large sample, so the STATISTICS, not the fit, set the error bar. Because only one state is prepared, nothing in the data says where |e> is: the centres come from the readout channel's stored pos_* monitors (an accepted single_shot_readout run), pinned into the fit, and the experiment REFUSES to run without them rather than inventing a second centre from a cloud that has only one. That reference is snapshotted into dataset.nc at acquisition time, so the analysis replays offline unchanged. It also REFUSES reset_method='active' BY NAME — active reset removes exactly the population being counted (use 'thermal', and raise thermalization_time_ns if you want a longer wait). Both refusals fire in define_sweep, before any hardware runs. TWO READINGS, AND THEY ARE NOT ONE QUANTITY BY TWO METHODS: p_e_given_g is COUNTED (nearest-centre hard assignment — population PLUS the readout overlap error) and pop_e_prep_g is the FITTED mixture weight (population with the overlap removed). The FITTED one is the chip fact n_th. ALSO: _scqat.per_qubit_results gains a keyword-only per_target_kwargs for knobs whose VALUE differs per qubit (the pinned centres are the first user), merged over the shared kwargs with per-target winning; and report.FIT_ONLY_QUANTITIES gains p_g_given_e, which it had been missing beside its three siblings. NO BREAKING CHANGES — everything here is additive (new modules, new monitors, a keyword-only parameter), nothing like v0.17.0's amplitude renames. No device action and no data action: run folders are immutable and datasets recorded before this release are unaffected."

[v0-17-0]
date = 2026-07-30
scqo = "v0.17.0"                 # the absolute amplitude axis + the unified amplitude window; two flux frames
lchqbdriver = "v0.17.0"          # volts->DAC-fraction on the flux wire; the amplitude DAC guard
lchqmdriver = "v0.17.0"          # the flux rail per port + the two frames; one home for QUA's amp() bound
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged since v0.13.0), and now further behind: its
                                 # sandbox experiments predate the amplitude renames below
scqat = "v0.6.0"                 # the twin (secondary) axis + the power-Rabi success-gate fix
notes = "REQUIRED: re-run the editable installs in ALL THREE venvs (.venv-qm, .venv-qblox, .venv-view) — scqo AND scqat both moved, and a stale editable reports the old version silently. BREAKING, three renames on the amplitude family (qubit_power_rabi, qubit_pi_pulse_error, readout_power, qubit_deterministic_benchmarking). (1) PARAMETER: qubit_power_rabi's `num_points` is now `num_amp_points`, matching the other three. Any ~/.scqo/config.toml, project params TOML or campaign plan carrying `num_points` under [qubit_power_rabi] now FAILS LOUDLY (Parameters sets extra=\"forbid\") — that is the intended outcome, not a silent revert to the default. The other experiments' `num_points` (resonator_spectroscopy, qubit_ramsey, qubit_relaxation, qubit_echo) is UNCHANGED. (2) DATASET AXIS: the swept ratio axis is `amp_prefactor` everywhere (was `amp_factor` on three of the four). Old run folders keep their old coord names and still load — they are immutable data — but a script that reads `ds['amp_factor']` from NEW runs must be updated. (3) FIT KEYS: `pi_amp_factor` / `opt_amp_factor` / `best_amp_factor` / `opt_factor` are all now `opt_amp_prefactor`; qubit_deterministic_benchmarking's redundant `opt_amp` is gone (it duplicated <knob>), and qubit_pi_pulse_error no longer emits its list-valued `amp_factors`/`gate_counts` (they duplicated dataset coords and counted as n_nonscalar in every campaign statistics table). NEW: every amplitude run's dataset.nc now carries a `digital_amp` coordinate — the ABSOLUTE amplitude, per target, behind each swept ratio point — so a saved run says what amplitude actually played without recovering the device snapshot; scqat draws it as a secondary axis on the figures and it survives into plotdata.nc. It is DIMENSIONLESS (a 0-1 fraction of full scale, the unit pi_amp/readout_amp carry), never volts and never dBm. TWO BEHAVIOUR CHANGES worth telling operators about: qubit_power_rabi's default max_amp_factor is 1.9, not 2.0 (2.0 sat exactly on QUA's unrepresentable amp() ceiling and the sweep emitted that endpoint); and a power-Rabi fit whose pi falls OUTSIDE the swept window now reports FAILED instead of silently returning the window edge as the answer. Both drivers now refuse an over-range amplitude BY NAME before building, naming the neutral knob to lower instead of dying inside the vendor compiler on `awg_gain`. Also in this release, from the parallel flux work: the neutral FluxSweepParameters drops its hardcoded +/-0.5 V rail bounds (they baked one vendor's direct-mode rail into the shared API and fired before any backend was consulted), and on Qblox flux volts are now converted to the DAC fraction the wire actually takes — before this, a requested 0.3 V left a QCM as 0.75 V and every fitted flux_offset was wrong by the module's full-scale factor."

[v0-16-0]
date = 2026-07-28
scqo = "v0.16.0"                 # deterministic benchmarking + the independent pi/2 knobs; campaigns; the depletion knob
lchqbdriver = "v0.16.0"          # active reset; mixer calibration; the WinError 87 silence; declines the two x90 knobs
lchqmdriver = "v0.16.0"          # TWO silent QM bugs fixed (virtual-detuning sign, stale pi/2 amplitude) + the x90 bindings
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged since v0.13.0)
scqat = "v0.5.0"                 # the deterministic-benchmarking estimator
notes = "READ THE TWO QM BUG FIXES FIRST — both are SILENT, both reached chipA, and both mean previously accepted calibrations are suspect. (1) VIRTUAL-DETUNING SIGN. QUA's frame_rotation_2pi rotates the ELEMENT FRAME, the opposite handedness to a pulse-axis phase (Qblox's X90(phase=...)), so an un-negated ramp puts the effective drive BELOW drive_freq_hz — the opposite of scqo's contract. scqo's shared estimate() writes drive_freq += (f_fit − applied) for BOTH backends, so on QM every accepted Ramsey update DOUBLED the residual detuning instead of cancelling it while the fit still converged and looked clean: chipA q1 walked +47.9 k → +95.7 k → +191.5 k Hz over three accepted runs. Both frame-ramping probes now carry the negation (qubit_ramsey, pair_qcq_zz_coupler_freq) and LCH_Ramsey/update.py ADDS d_f01 to match. The ZZ probe's stakes were lower — un-negated it reported zz = f_fit − detuning with the sign flipped against the physical residual ZZ, but the zero crossing feeding its only writeback (idle_flux on the coupler's flux channel) is EXACTLY invariant under zz → −zz (verified over 2000 noisy trials, max delta 0.000e+00 V, outcomes identical), so no accepted update was ever wrong there; only the reported and plotted sign changes. Do NOT copy the official nodes' sign into a probe: 06a_ramsey.py subtracts to compensate for its own un-negated ramp, and 19_zz_off_jazz.py takes only argmin|zeta|, which cannot tell the difference. (2) STALE pi/2 AMPLITUDE. QM's x90 was unreachable under scqo — no experiment calibrated it and no knob set it — so it simply went stale: chipA q1 ran x180 = 0.28619 with x90 = 0.07302 and −x90 = 0.09329 against a correct 0.14310, a 46-degree 'pi/2' capping Ramsey contrast at sin^2(46 deg) ~ 0.5 while every fit converged cleanly. FEATURE — DETERMINISTIC BENCHMARKING, AND THE pi/2 BECOMES ITS OWN KNOB. New experiment qubit_deterministic_benchmarking: plays ONE target gate N times and sweeps N so a small per-gate over/under-rotation accumulates into a resolvable fringe, repeated across an amplitude-scale sweep whose fringe rate crosses zero at the correct amplitude. Far more sensitive than power Rabi, which sees the rotation error once; run it after power Rabi has the amplitude roughly right. It calibrates whichever gate is benchmarked, which is why the catalog gains TWO knobs on the drive channel: pi_amp_x90 and drag_beta_x90. THE DESIGN POINT MOST LIKELY TO BE 'FIXED' BACK: the pi/2 is INDEPENDENT, never pi_amp/2. A derived x90 cannot be calibrated in its own right, and amplitude response is not perfectly linear — which is precisely the error this experiment measures. LCHQMDriver's set_pi_amp therefore has NO lock_x90 flag at all: once x90 owns a knob the flag is self-contradictory, since it existed only to paper over x90 having none. What replaces it is a table (_AMP_NODES) mapping each family to its storage nodes — one operation, one family, no mode flag and no operation-name ladder. The x90 family deliberately includes −x90_DragCosine, which carries its OWN amplitude (only its alpha/detuning are QUAM references; the negative sense comes from axis_angle = pi), so the two pi/2 gates cannot drift apart. Relatedly, _set_op_alpha no longer takes force=True: forcing a literal over a node whose alpha is a QUAM REFERENCE silently SEVERS the alias so later writes stop propagating, and −x90_DragCosine.alpha is exactly such a reference. The qualibrate power_rabi node keeps its update_x90 GUI option, now expressed as what it always was — a node policy — by composing the two independent writes rather than passing a mode flag into quam_fields. ON QBLOX BOTH NEW KNOBS ARE Unrealized, following the rule drag_beta already states: a vendor slot that exists with no experiment writing it gets declined, not bound. pi_amp_x90's slot DOES exist (PiHalfProperties.amp90) but no Qblox probe writes it — qubit_deterministic_benchmarking is QM-only, as is the rest of that family — so binding an amplitude nothing calibrates would claim a calibration the backend cannot make. Qblox otherwise DERIVES X90 from rxy.amp180 as amp180*theta/180, so pi_amp alone governs both gate widths there. Promotion means moving pi_half from FluxTunableTransmonElement up to the LCHTransmonElement base (fixed-frequency chips need it too) and binding element.pi_half.amp90; drag_beta_x90 promotes with drag_beta, since rxy carries a single beta. Note each declined knob ALSO needs a concrete raising property pair — make_view_base declares an abstract property for every catalogued knob, so declaring one Unrealized without implementing it makes QbloxDriveChannel uninstantiable. THE ESTIMATOR IS scqat's, NOT SCQO's (scqat v0.5.0). SCQO's estimate() delegates via per_qubit_results, per _scqat.py's rule that 'SCQO does not fit anything itself'. Two silent defects were fixed in the estimator on the way in: it read raw I with no axial reduction (the readout blobs sit at an arbitrary rotation in the IQ plane, so raw I is an arbitrarily-signed, arbitrarily-scaled trace — it now goes through reduced_signal like every other coherent-drive estimator), and damped_cosine_zero_phase fixes phi=0 with the fit bounding A >= 0, so a trace that starts LOW collapsed onto the flat A ~ 0 bound. Since the sequence starts in |0>, N=0 is an extremum by construction, so each trace is now oriented to start high. Both failed by returning a clean-looking opt_factor = 1.0 rather than raising, because curve_fit is wrapped in try/except. KNOWN LIMIT, documented in the estimator's tests: the sign heuristic pivots at a = 1.0 rather than the true optimum, so points between the two are signed wrongly and bias the crossing slightly (~0.2% on synthetic data). It CONVERGES across runs — the next run starts nearer 1.0 — which is why it is tolerated rather than redesigned. FEATURE — CAMPAIGNS (see SCQO CLAUDE.md and TUTORIAL for the full surface). A campaign is an ordered list of experiment STEPS walked N times, each (repeat, step) a full run() with its own folder, dataset, fit and timestamp. Finalization now lives in a `finally`, so the manifest is written on EVERY exit path — normal stop, Ctrl-C anywhere (including the cadence sleep, where a period_s campaign spends most of its wall clock and where an interrupt previously left status='running' forever), or an unexpected crash. An interrupted repeat KEEPS the steps that already ran, marked `partial` and counted in repeats_partial, never in repeat_done. statistics.png is rendered on finalize (lazy Agg matplotlib; a figure failure warns and never fails the campaign). single_shot_readout also now reports the Gaussian-fit populations pop_e_prep_g / pop_g_prep_e beside the COUNTED p_e_given_g / p_g_given_e — they are NOT one quantity by two methods and never share a key; the gap between them is roughly the discrimination error. On chipA the count's 125% scatter collapsed to 22% on the fit, and across two campaigns the count mean was flat (1.86% → 1.88%) while the fit rose 48%. TUTORIAL gains a 'Which readout number to trust' block. ALSO IN THIS COMBO: reset_method gains 'active' and an unrealizable method must REFUSE by name rather than downgrade (Qblox realizes it behind one door with four refusals; LCHQMDriver raises); the post-readout photon-depletion wait becomes the governed knob readout_depletion_s with a vendor home on BOTH backends, resolved only via _depletion.depletion_wait_ns; Qblox gains automatic mixer calibration for the RF modules, verified on hardware, built around sideband_cal() returning success while changing nothing on roughly half to three-quarters of calls (hence verify-then-retry, never trust-a-condition); the WinError 87 traceback that ended every scqo run on Windows is silenced at its source in the Qblox transport rather than in SCQO's vendor-neutral CLI; and the datasette canned queries are pointed at runs.targets after the greenfield rename left all four failing with 'no such column: qubits'. UPGRADE: checkout v0.16.0 in SCQO + LCHQBDriver + LCHQMDriver, and scqat at v0.5.0. scqat is REQUIRED, not optional: SCQO's qubit_deterministic_benchmarking imports scqat.estimators.qubit_deterministic_benchmarking, so a scqat CHECKOUT left at v0.4.0 fails at run time with ModuleNotFoundError — this is what reddened SCQO PR #13's CI, whose workflow clones scqat main as a sibling. An EDITABLE install is different and more insidious: it serves live source, so the module is already there and only the recorded VERSION is stale, which means `scqo doctor` reports a number that has nothing to do with the code being run. Audited at release time: .venv-view, .venv-qm and .venv-qblox were ALL still reporting scqo 0.14.0 (and .venv-view scqat 0.1.6) while running current source — v0.15.0's identical instruction had not been carried out in any of them. So: re-run `uv pip install -e . --no-deps` in every editable env (LCHQMDriver's env additionally needs `uv pip install -e D:\\github\\SCQO -e D:\\github\\scqat` if a bare `uv run` has pruned them — its scqo dep is an optional extra, so always `uv run --no-sync` there). DEVICE ACTION REQUIRED ON QM, and it is the reason to upgrade promptly: any qubit whose drive_freq_hz was set by an ACCEPTED Ramsey update before this release may carry a doubled residual detuning, and any whose pi/2 was assumed to be half the pi may be running a short x90. Re-run qubit_ramsey and qubit_deterministic_benchmarking (target_gate=x90) per qubit and accept. No data action — run folders are immutable; datasets recorded before this release are unaffected, though a ZZ map's reported zeta sign flips and its saved figures keep the old one."

[v0-15-0]
date = 2026-07-26
scqo = "v0.15.0"                 # swept time axes are whole nanoseconds; trends read accepted knob suggestions
lchqbdriver = "v0.15.0"          # use_state_discrimination end to end; grid guards on the seconds knobs + a compiling regression net
lchqmdriver = "v0.15.0"          # takes the discriminator solve from scqat, local copy deleted
scqo-contrib = "v0.12.0"         # still NOT in the combo
scqat = "v0.4.0"                 # the g/e discriminator solve promoted into tools/
notes = "TWO THINGS IN ONE COMBO — a state-discrimination FEATURE and an off-grid-time BUG FIX. Cut as a MINOR (not the v0.14.1 the bug fix alone was drafted as) because the feature is user-visible and the tags here are immutable. FEATURE — STATE DISCRIMINATION. `use_state_discrimination` works end to end on the Qblox backend and is VERIFIED ON chipA HARDWARE (discriminated qubit_relaxation and qubit_ramsey both ran clean, which is also what settles that the rotation SIGN is correct — the one thing offline tests could not prove). `readout_rotation_rad` / `readout_threshold` are now REALIZED on Qblox (`element.measure.acq_rotation` / `acq_threshold`), superseding the earlier 'Qblox declares all three Unrealized' position; `readout_rus_threshold` stays Unrealized deliberately — Qblox has no RUS threshold knob, it does conditional playback instead. The g/e discriminator solve is promoted into `scqat.tools.compute_ge_discriminator` (scqat v0.4.0) and BOTH drivers now consume that one implementation — LCHQMDriver deleted its local copy. THE DESIGN POINT MOST LIKELY TO BE 'FIXED' BACK BY SOMEONE READING ONLY THE QM SIDE: accumulate a rotation proposal ONLY when the vendor's rotation feeds back into the acquired data. QM's `integration_weights_angle` is folded into the integration weights, so it rotates every acquisition and `current - delta` converges. Qblox's `acq_rotation` is read on the ThresholdedAcquisition path ONLY, so plain integrated blobs come back in the raw frame forever, the same delta is measured every run, and accumulating has no fixed point — chipA walked 0 -> -0.388 -> -0.788 -> -1.177 rad over three accepted runs before this was caught. Qblox therefore proposes ABSOLUTELY (`-delta`). The rule is written into `scqat/tools/ge_discriminator.py`'s ANGLE docstring and both drivers' `update()`. Angles fold at the VENDOR BOUNDARY, not in the solve: the Qblox sequencer refuses `acq_rotation` outside [0, 360] deg while the neutral field keeps (-pi, pi], and both directions wrap in the view — a test fixture calibrated with a POSITIVE rotation is what let the sign error reach hardware, so fixtures for this knob must be NEGATIVE, which is what the solve returns. Active reset is the next piece of work and its hook already exists: widen `reset_method: Literal[\"thermal\"]` in scqo/experiments/_capabilities/qubit_reset.py. BUG FIX — OFF-GRID SWEPT TIMES. `qubit_relaxation` on chipA/Qblox died at compile time: 'Attempting to use a time value of 2004649.6799999997 ns ... multiples of 1 ns (tolerance: 1e-03 ns)'. NOT a v0.14.0 regression and NOT the thermalization knob (chipA's was a clean 0.001 s): `define_sweep` built its time axes with np.linspace over floats, so the failing window (min_wait_ns=16, max_wait_ns=100000, num_points=51) stepped by 1999.68 ns and 48 of its 51 points were fractional. Those times accumulate into absolute operation timestamps and qblox_scheduler refuses anything off its 1 ns GRID_TIME (tolerance 1.1e-3 ns). The defect is OLD and systemic — the stock defaults of qubit_relaxation (3999.68 ns step), qubit_echo (7999.36) and qubit_ramsey (79.68) were all off-grid too — and QM had been hiding it for every experiment by re-rounding each axis to clock cycles (np.round(wait_ns/4), /8 for echo's two arms). Qblox is simply the backend that enforces the grid. FIX: new `scqo/experiments/_time_grid.py::time_axis_ns` produces a UNIFORM, INTEGRAL axis — start ceil'd, STEP floor'd — used by all six time-swept experiments (qubit_relaxation, qubit_echo, qubit_ramsey, qubit_relaxation_flux, qubit_echo_flux, pair_zz_coupler). Uniform matters as much as integral: the Qblox probes rebuild the axis as linspace(first, last, size), so a merely per-point-rounded axis is no longer evenly spaced and gets re-expanded straight back off-grid. The window is now a BOUND, not a target — the last point can land up to one step short of max_*_ns, which is preferred over silently waiting longer than the caller asked for; an over-dense request (span too short for num_points at 1 ns) is REFUSED with the fix named, a case that was already degenerate (QM collapsed it into duplicate clock counts). The echo family uses grid_ns=2: those probes schedule two `tau / 2` arms, so an odd total would hand the instrument half a nanosecond. SECOND, INDEPENDENT INLET FIXED: LCHQBDriver wrote its seconds knobs raw. `thermalization_time_s` is now ROUNDED onto the 1 ns grid by the new `snap_ns` (matching the policy LCHQMDriver's set_thermalization_time already decided — round, do not refuse, because a T1-derived wait is never on the grid by luck and the calibration loop must be able to write its own result), which matters because the very next accepted `thermalization_factor x t1_s` proposal would have crashed the same way; the per-run thermalization_time_ns override goes through the same snap; `resonator_relaxation_time_ns` is snapped at the probe boundary (and converted with / 1e9, never * 1e-9, per the repo's own float-exactness rule); and `pi_duration_s` now REFUSES a non-whole-nanosecond length rather than accepting it and crashing at compile — refused, not rounded, because the stored pi_amp was calibrated against a specific pulse length. WHY IT WAS NEVER CAUGHT OFFLINE: tests/test_probe_surface.py stops at 'a Schedule was built', and the grid check only runs inside the compiler. LCHQBDriver/tests/test_time_grid.py now COMPILES every time-swept probe with SerialCompiler — the same call HardwareAgent.run makes — and carries a self-check asserting each case's naive linspace step really was fractional (trimming num_points to 7 makes linspace(16, 100000, 7) land on an integer 16664 ns step and the case would pass for the wrong reason). Both suites compare against an ABSOLUTE tolerance: np.allclose's default rtol=1e-5 calls 40012.8 ns equal to 40013 and would make every such assertion vacuous. VERIFIED: 639 offline tests (scqo 473 / lchqmdriver 105 / lchqbdriver 61), the pre-fix axis reproduced and confirmed to still raise under the new compile test, and QM's cycle counts checked to stay distinct and monotonic on the new axes. UPGRADE: checkout v0.15.0 in SCQO + LCHQBDriver + LCHQMDriver, and scqat at v0.4.0 — scqat is REQUIRED, not optional, this time: both drivers import `scqat.tools.compute_ge_discriminator` and LCHQMDriver has deleted its local copy, so an env left on scqat 0.3.0 fails at import. Then re-run `uv pip install -e . --no-deps` in every editable env (LCHQMDriver's env additionally needs `uv pip install -e D:\\github\\SCQO -e D:\\github\\scqat` if a bare `uv run` has pruned them — its scqo dep is an optional extra, so `uv run` syncs them away; always `uv run --no-sync` there). No data action and no device-config change; datasets recorded before this release carry the old fractional time coords, which were never what the instrument played."

[v0-14-0]
date = 2026-07-26
scqo = "v0.14.0"                 # the qubit_reset capability + the thermalization_time_s pushed knob
lchqbdriver = "v0.14.0"          # element.reset.duration binding; Reset added to power_rabi + ramsey
lchqmdriver = "v0.14.0"          # Thermalizing*Transmon classes + q.thermalization_time_ns binding
scqo-contrib = "v0.12.0"         # still NOT in the combo (unchanged since v0.13.0)
scqat = "v0.3.0"                 # unchanged (no estimator touched)
notes = "QUBIT RESET BECOMES A CAPABILITY. Every experiment that pulses a qubit and reads it out assumes shot-to-shot independence, but the reset that buys it was invisible and hard-coded: on QM 12 scqo shells passed reset_type='thermal' and 4 more probes called qubit.reset('thermal') inline, so the wait was always QUAM's derived thermalization_time_factor * T1; on Qblox it was element.reset.duration (500 us in the lab config) — except qubit_power_rabi and qubit_ramsey, which had NO Reset at all and leaned on a hard-coded trailing IdlePulse(10 us), shorter than several measured T1s and 50x shorter than the rest of the suite. NEW: `_capabilities/qubit_reset.py` — QubitResetParameters (reset_method: Literal['thermal'] = the extension point active reset will widen, plus the per-run override thermalization_time_ns) and reset_wait_ns(), the ONE precedence point both drivers call so an override cannot come to mean different things per backend. Derived tag `qubit_reset`, appended LAST in _derived_tags so existing tag lists keep their order; carried by all 16 core experiments that already reset (resonator/punchout stay tag-less — no qubit pulse, nothing to reset). NEW PUSHED KNOB: `thermalization_time_s` on the DRIVE channel (q1_xy), absolute seconds, portable — declared after pi_duration_s and before the drive_amp/drive_power_dbm chain pair so push order is unaffected. qubit_relaxation now writes TWO roles from one fit (the placement rule's 'classify each USE'): t1_s stays a fact -> physical.json, and thermalization_factor x T1 (new Parameters field, default 10.0) becomes the knob -> scqo_state.json + pushed. Manual bring-up before any T1 exists is `scqo set q1.thermalization_time_s=...`, which needed no new code (the qubit closure routes it to q1_xy). VENDOR HOMES: Qblox maps 1:1 onto element.reset.duration (both absolute seconds — the one field needing no conversion), and because Reset() compiles to IdlePulse(reset.duration) every Qblox probe honours it with no probe-side argument. QM has no writable absolute field — thermalization_time is a read-only factor*T1 property — so LCHQMDriver adds ThermalizingFixedFrequencyTransmon / ThermalizingFluxTunableTransmon storing thermalization_time_ns and overriding the property to prefer it (falling back to the derived value when scqo has never calibrated), which keeps qubit.reset('thermal') correct in all 16 QM probes with ZERO probe or shell edits. `Unrealized` was evaluated for QM and REJECTED: _set_knob pushes before recording with no NotImplementedError handling (unlike _seed_push), so the knob would be unsettable, and _seed_pull builds its cache from the vendor snapshot alone, so a stored value would read back None every session — a knob needs a real vendor home. The per-run override is a recorded set->revert bracketing probe() on QM (the wait is baked into the compiled program at build time) and the whole acquire on Qblox (the scheduler expands Reset during compilation inside run()); it writes the vendor tree directly, leaving no ChangeRecord and no store row. MIGRATION REQUIRED on QM: each qubit serializes its own __class__, so a device only gets an absolute wait once its state.json qubits name a Thermalizing*Transmon; until then set_thermalization_time raises NotImplementedError naming the fix, and the derived factor*T1 keeps working. This is a per-device-config edit, NOT the quam_config/my_quam.py qubit_type toggle (that is the lab's charge-tunable switch and is governed separately). ALSO: `scqo state` column width is now derived from the widest field name — at the hardcoded 20 the 21-character fields (readout_integration_s, and now thermalization_time_s) collided with their left neighbour. VERIFIED: 605 offline tests (scqo 454 / lchqmdriver 105 / lchqbdriver 46) plus a simulated end-to-end CLI walkthrough (run -> two suggestions -> accept -> both stores -> scqo set -> history). UPGRADE ACTIONS: (1) `git fetch --tags && git checkout v0.14.0` in SCQO + both drivers (scqat stays v0.3.0; do NOT install scqo-contrib, still v0.12.0 and incompatible). (2) RE-RUN `uv pip install -e . --no-deps` in every editable env — an editable install serves live source but freezes its recorded VERSION at install time, so `scqo doctor` otherwise keeps reporting the old number (this is what v0.13.1 existed to fix). (3) QM devices only: migrate each device config with `python scripts/migrate_thermalizing_transmon.py <backend_config_dir>` in LCHQMDriver — it backs the file up to `<name>.pre-thermalizing.bak`, offers `--dry-run`, edits only the `__class__` lines by targeted string replacement (no JSON round-trip, so formatting survives), asserts the root `__class__` and top-level key set are unchanged, and is idempotent. chipA and 5Q4C are ALREADY migrated. An unmigrated device is not broken — it keeps running on the derived factor x T1 and only refuses `scqo set <q>.thermalization_time_s`, naming the script. No data action: run folders are immutable, the index is unchanged."

[v0-13-1]
date = 2026-07-25
scqo = "v0.13.1"                 # version-string fix; code identical to v0.13.0
lchqbdriver = "v0.13.0"          # unchanged (drivers version by tag, pyproject stays 0.1.0)
lchqmdriver = "v0.13.0"          # unchanged
scqo-contrib = "v0.12.0"         # still NOT in the combo (see v0-13-0)
scqat = "v0.3.0"                 # unchanged
notes = "PATCH: v0.13.0 shipped with pyproject still declaring 0.12.0, so a correctly upgraded install REPORTED 0.12.0 — exactly the 'scqo doctor's version can lie' trap that editable installs already make easy to hit. Caught while performing the upgrade itself. Code is byte-identical to v0.13.0; only the declared version changes. The pushed v0.13.0 tag was deliberately NOT moved (standing rule). UPGRADE: checkout v0.13.1 in SCQO only, then re-run `uv pip install -e . --no-deps` in every editable env — the .dist-info metadata is frozen at install time, so without the reinstall the version keeps reporting stale no matter which tag is checked out."

[v0-13-0]
date = 2026-07-25
scqo = "v0.13.0"                 # the greenfield device model replaces the component model
lchqbdriver = "v0.13.0"          # per-kind channel views + kind-keyed fieldmap
lchqmdriver = "v0.13.0"          # same, plus the composite (pair) knob surface
scqo-contrib = "v0.12.0"         # NOT IN THIS COMBO — deliberately left behind, see below
scqat = "v0.3.0"                 # unchanged (estimators are model-agnostic)
notes = "THE DEVICE MODEL IS REPLACED (docs/greenfield-schema.md is the spec). A device is no longer a flat set of COMPONENTS with two category slots; it is four kinds of ENTITY: MODES (quantum degrees of freedom — transmon/flux_transmon/fluxonium/cavity/resonator), COMPOSITES (named mode groups with joint physics — qubit_pair, cat_system), LINES (one physical control path reaching the sample) and CHANNELS (one signal of one kind riding a line). components.toml is schema 3 with [modes]/[composites]/[lines]/[channels]; a line's RIDER LISTS mint the channels (readout=['q1','q2'] on one line IS frequency-multiplexed readout — the topology the old schema could not express) plus each readout target's <t>_res resonator mode. Design values move OUT of the roster into a sibling design.toml (entity-named tables, context-free vocabulary, editable after the production cut). FIELD ROUTING is now per-field by ROLE: fact -> physical.json, knob -> scqo_state.json + pushed, monitor -> scqo_state.json never pushed; both stores are schema 3 with top-level 'values' (scqo_state.json's old 'config' key is gone) and history rows say entity/kind (was component/category). KNOBS RE-HOME onto channels: q1.readout_freq -> q1_ro.readout_freq_hz, q1.pi_amp -> q1_xy.pi_amp, q1.idle_flux_v -> q1_z.idle_flux; measured flux transfer functions v_offset_v/v_per_phi0_v -> q1_z.flux_offset/flux_per_phi0; readout_fidelity DELETED (per-state fidelity_g/fidelity_e); readout_pos_* -> pos_*; the pair's coupler_decouple_v/coupler_interaction_v are gone — a coupler's standing bias is idle_flux on ITS OWN flux channel and gate points are per-operation composite knobs (<op>_coupler_flux). Addressing keeps the qubit-level sugar (scqo set q1.pi_amp routes through the closure to q1_xy). NEW: `scqo device freeze` writes components.lock and the append-only production cut becomes enforceable (superset by signature; retire, never delete); doctor gains model witnesses (unreachable modes, unbiasable couplers, design coverage, lock drift, roster-vs-vendor inventory, line->port wiring, and a live z element on a fixed-frequency qubit = FAIL). DRIVER CONTRACT CHANGE: build_backend(cfg, setup, roster) — a driver serves a view PER CHANNEL ENTITY and resolves names through the roster. UPGRADE REQUIRES: rewrite each device's components.toml to schema 3 and add its design.toml (chipA and 5Q4C are done; old files kept as *.v1.bak); the stores are FRESH-START (pre-v3 files are archived aside as *.v2.bak on first contact — knobs reseed from the vendor under state_sync='pull', monitors are re-measured, facts re-accumulate; a device with measured physics worth keeping can carry it across, since only the two flux-transfer fields rename); user parameters.toml needs no change. DATA: run folders are immutable and stay findable; result.fit keys follow the new spellings for NEW runs only. VERIFIED BEFORE THE CUT: 590 offline tests across the three repos (scqo 450 / lchqbdriver 43 / lchqmdriver 97+4skip), plus REAL-CONFIG and HARDWARE checks on BOTH devices — doctor + pull-mode seeding against the actual vendor configs, and a live resonator-spectroscopy smoke run on chipA and 5Q4C. SCQO-CONTRIB IS DELIBERATELY NOT IN THIS COMBO: its sandbox experiments still import the deleted component model, so it stays at v0.12.0 and is KNOWN-INCOMPATIBLE with scqo v0.13.0 — do not install it into a v0.13.0 env until it is re-pointed (nothing depends on it; the catalog simply loses the contrib group, which _discover() already tolerates). STORE CARRY-OVER: not performed. Each context's pre-cutover stores were archived aside as *.v2.bak on first contact (the fresh-start policy) and the handful of measured facts are being re-measured rather than translated; the archived files remain on disk."

[v0-12-0]
date = 2026-07-24
scqo = "v0.12.0"                 # _capabilities package (state_readout + flux) + derived catalog tags; flux naming unified
lchqbdriver = "v0.12.0"          # no-change re-tag (zero references to the renamed fields; flux probes already read flux_bias_v)
lchqmdriver = "v0.12.0"          # flux probes/shells emit+read flux_bias_v; both flux shells pass use_state_discrimination through
scqo-contrib = "v0.12.0"         # no-change re-tag
scqat = "v0.3.0"                 # NEW: coherence-flux estimators' coord/keys flux_amp -> flux_bias (THE COMBO MUST PIN IT)
notes = "Experiment capabilities become CODE, and catalog tags are DERIVED from them. New scqo/experiments/_capabilities/: one module per capability owning the canonical Parameters mixin + contract fragment + simulate/estimate helpers — state_readout.py (StateReadoutParameters carries THE one use_state_discrimination field text; STATE_ALT; state_row/readout_vars/signal_rename) and flux.py (FluxSweepParameters min_flux_v/max_flux_v/num_flux_points with +-0.5 V rails; the canonical probe axis FLUX_AXIS='flux_bias_v'; FluxComponentParameters + the foreign_flux_source record-only guard). catalog() entries gain 'tags' derived from Parameters-mixin subclassing (state_readout: relaxation/echo/ramsey/power_rabi/sqrb/relaxation_flux/echo_flux; flux: resonator_spectroscopy_flux/qubit_spectroscopy_flux_pulse/relaxation_flux/echo_flux; ZERO tags is legitimate — a new experiment may not be classifiable yet, and no test may demand tag completeness); the CLI menu and --help epilog render them; declared-string tags do not exist, so a tag cannot lie or rot (tests/test_capabilities.py locks the census, the canonical field texts, and the FLUX_AXIS contract). REAL WIRING: qubit_sqrb + qubit_relaxation_flux + qubit_echo_flux now support state discrimination end-to-end (simulate state branch, state->signal, contract accepts state — the flux pair's old contracts REJECTED a state dataset; the QM flux shells now pass the flag through to probes that already implemented the state path); qubit_pi_pulse_error DROPS use_state_discrimination (its QM shell hardcodes False, matching the DRAG pair — the field was a dead promise). RENAME (no alias, the standing rule): qubit_relaxation_flux/qubit_echo_flux parameters min_flux_amp_in_v/max_flux_amp_in_v -> min_flux_v/max_flux_v and sweep axis flux_amp -> flux_bias_v; scqat's coherence-flux estimators (bumped to v0.3.0, THE COMBO MUST PIN IT) consume coord flux_bias and return results key flux_bias; new runs of the pair record fit key flux_bias_v (old run folders immutable, keep flux_amp, no reindex — index stays v7). UPGRADE REQUIRES: git fetch --tags && git checkout v0.12.0 in the four scqo-versioned repos AND scqat at v0.3.0, then re-run `uv pip install -e . --no-deps` in each editable env; RENAME user parameters.toml keys under [qubit_relaxation_flux]/[qubit_echo_flux] (min_flux_amp_in_v -> min_flux_v, max_flux_amp_in_v -> max_flux_v) and DELETE any use_state_discrimination key under [qubit_pi_pulse_error] — stale keys fail loudly (extra='forbid'). BEHAVIOR: the coherence pair's flux window is now rail-bounded to +-0.5 V (was unbounded); carrier field-description texts unify on the canonical mixin texts; catalog entries gain the additive 'tags' key. DATA CAVEAT: none — additive schema, no reindex."

[v0-11-2]
date = 2026-07-24
scqo = "v0.11.2"                 # stored-position consumption + governed readout discriminator; qubit_spectroscopy_flux -> _pulse
lchqbdriver = "v0.11.2"          # flux wrapper rename + readout discriminator fields declared Unrealized
lchqmdriver = "v0.11.2"          # QM discriminator realizers (governed) + stage-3 wrapper threading + flux wrapper rename
scqo-contrib = "v0.11.2"         # no-change re-tag
scqat = "v0.2.1"                 # NEW: stored-position consumption, flux ref_scope, mean-shift discrimination centre
notes = "The readout state->IQ map becomes a first-class, governed calibration. STAGE-3: use_state_discrimination on qubit_power_rabi/ramsey/echo/relaxation returns the FPGA-discriminated averaged state instead of I/Q (DatasetContract.alt_variables accepts (I,Q) OR state; the estimator consumes the pre-reduced signal path, reduction_method='signal', no iq_plane). single_shot_readout measures the |g>/|e> blobs, always plots them AS MEASURED (it never mutates the readout frame mid-run), stores the measured centres as the readout_pos_* reference, and PROPOSES the readout discriminator as GOVERNED suggestions on three NEW pushed, non-portable ReadableTransmon fields readout_rotation_rad / readout_threshold / readout_rus_threshold (QM realizes them on the readout op's integration_weights_angle [absolute rad] / threshold / rus_exit_threshold; Qblox declares them Unrealized -- acq_rotation/acq_threshold exist but no Qblox single_shot yet, and Qblox has no RUS). This REPLACES both the qualibrate 07_iq_blobs node AND the short-lived out-of-band calibrate_discriminator flag (REMOVED): the phase/threshold are now reviewed with scqo accept and confirmed by re-running, like drive_freq/pi_amp. The discrimination CENTRE is now the density PEAK: scqat's discriminate.py refines the coarse-histogram-argmax seed onto each blob's mode by nearest-centre-confined mean-shift (was frozen vary=False at the ~sigma/3 bin, sitting a fraction of sigma off the peak on low-separation/skewed readout; validated on real 0.88-fidelity 5Q4C data, fidelity 0.876->0.891). CONSUMPTION: the stored readout_pos_* now feed the IQ->1-D reductions -- scqat (bumped to v0.2.1, THE COMBO MUST PIN IT) attaches them to the acquired dataset as per-target ref_pos_* variables at acquisition time (Experiment.attach_readout_positions on qubit_spectroscopy + the coherent-drive four; persisted in dataset.nc so offline replay is faithful), and reduced_signal prefers them (priority explicit kwarg -> stored positions -> PCA): radial reads the stored ground point (ref_source='stored'), axial takes the measured g->e axis (reduction_method='positions'), and the shared iq_plane panel draws the two stored blobs + the g->e vector. RENAME: qubit_spectroscopy_flux -> qubit_spectroscopy_flux_pulse (no alias; the acquisition style becomes experiment identity, the resonator_spectroscopy_power_amp/_chain precedent) -- the name is the probe contract (flux pulsed only during the drive, readout at idle flux every slice; both QM and Qblox probes already conform), so the map is reduced against ONE global IQ reference (scqat ref_scope='global': the whole-map complex median, n_flux x more samples, the same idle-flux condition single_shot calibrates). A DC-held flux-spectroscopy variant would be a separate experiment keeping the per-slice reference. Yuhowlin's PRs #12/#16/#21 superseded-with-credit (closed). Drivers: lchqmdriver + lchqbdriver are REAL tags (QM realizers / Qblox Unrealized + both flux wrappers renamed); scqo-contrib is a no-change re-tag. UPGRADE REQUIRES: git fetch --tags && git checkout v0.11.2 in the four scqo-versioned repos AND scqat at v0.2.1, then re-run `uv pip install -e . --no-deps` in each editable env (refreshes the version scqo doctor reports and registers scqat's new reductions). RENAME user parameters.toml [qubit_spectroscopy_flux] sections to [qubit_spectroscopy_flux_pulse] (old runs stay findable under the old name); drop any calibrate_discriminator key (the parameter is gone -- a stale key fails loudly). QM: pull-mode seeds the three new discriminator fields from each readout op (chipA already carries integration_weights_angle/threshold/rus_exit_threshold from 07_iq_blobs); no config edit. No data action: index stays v7, old run folders immutable. BEHAVIOR: single_shot_readout no longer writes the vendor discriminator itself -- run it, scqo accept the readout_rotation_rad/readout_threshold suggestions, then re-run to confirm; a re-fit of the SAME coherent-drive raw data now takes its reduction axis from the stored blobs when present (deterministic g->e sign) instead of per-run PCA -- same physics, and identical when no positions are stored. DATA CAVEAT: none -- additive to the schema."

[v0-11-1]
date = 2026-07-23
scqo = "v0.11.1"                 # coherent-drive experiments reduce IQ via scqat axial (not raw I); flux-arch trust gates
lchqbdriver = "v0.11.1"          # no-change re-tag (probe-only; coherent-drive probes already emit I/Q)
lchqmdriver = "v0.11.1"          # no-change re-tag (probe-only; coherent-drive probes already emit I/Q)
scqo-contrib = "v0.11.1"         # no-change re-tag
scqat = "v0.2.0"                 # NEW: IQ->1D radial/axial reduction + flux-arch seed/gates + shared iq_plane panel
notes = "IQ->1-D signal reduction becomes a shared scqat concern and the coherent-drive experiments stop discarding a quadrature. scqat (bumped to v0.2.0, THE COMBO MUST PIN IT -- the experiments' estimate() now calls scqat.tools.iq_reduce, absent in the v0.1.8 tag) gains two geometry-matched reductions: radial |IQ-ref| (the spectroscopy family; ref = off-resonance/ground median, per-slice for flux maps) and axial, the signed projection onto the |0>-|1> axis (the coherent-drive family; axis resolved angle->positions->PCA). The four coherent-drive experiments (qubit_power_rabi/qubit_ramsey/qubit_echo/qubit_relaxation) now hand complex I/Q to the estimator -- reduced via axial, PCA at bring-up -- instead of rename({'I':'signal'}) (raw I lost contrast as cos(readout_rotation), near-zero when the state separation sits in Q); estimate() renames only the sweep axis, and simulate() emits faithful IQ (population placed at a random readout rotation with noise in both quadratures, so the offline sims exercise the reduction instead of hiding it). The estimators still accept a pre-reduced real 'signal' unchanged (the FPGA-discriminated 'state' path). scqat's qubit_spectroscopy_flux / qubit_flux_arch additionally gain a wrong-basin GUARD: the transmon-arch fit seeds its flux period from the arch-top curvature (top-only bring-up windows have period >> the swept span, where the old period=span seed converged to a spurious basin) and GATES success on the arch top being observed inside the swept frequency window (+15% margin) and the curve tracking the branch points (rms residual <= span/4); the flux figures now draw the per-slice REDUCED map (a weak line is invisible under the raw |IQ| ground offset + chain ripple). All seven IQ-reducing estimators emit a shared 'iq_plane' figure (the raw IQ cloud + the reference actually used: radial point / per-slice trajectory / axial axis, titled with the reduction kind), and stamp reduction_method / reduction_angle (+ ref_source) into metadata. Drivers unchanged: every coherent-drive experiment on LCHQM/LCHQB is probe-only and already saves buffered/averaged I/Q (use_state_discrimination False), and simulate() is inherited from scqo, so lchqbdriver/lchqmdriver/scqo-contrib get no-change re-tags at v0.11.1 (an estimation-only combo, the v0.10.2 pattern). UPGRADE REQUIRES: git fetch --tags && git checkout v0.11.1 in the four scqo-versioned repos AND scqat at v0.2.0, then re-run `uv pip install -e . --no-deps` in each editable env (refreshes the version scqo doctor reports -- the frozen .dist-info is not updated by git pull -- and registers scqat.tools.iq_reduce). No config or data action: no schema change and no reindex (index stays v7); old run folders are immutable and stay findable. BEHAVIOR: additive to the schema, but a re-fit of the SAME raw data shifts results two ways. (1) Coherent-drive: the fit now uses the full IQ (axial/PCA) rather than raw I -- it recovers contrast where the readout is not aligned to I (validated on real QM 5Q4C data: up to 2.16x on power_rabi) and is unchanged where it already was; the extracted pi_amp / T1 / T2* / T2echo / detuning are the same physics at higher SNR. (2) qubit_spectroscopy_flux now GATES the arch fit: a fit whose top was never observed in the swept window (or that misses the branch points) is reported FAILED (outcome FAILED, no writeback) instead of a spurious 'success', so an in-window arch that previously read a wildly-off f01_max/Ej_sum now either fits correctly or fails honestly. DATA CAVEAT: pre-v0.11.1 qubit_spectroscopy_flux runs may carry a spurious arch fit (e.g. run 20260723-134245 read f01_max 6.27 GHz / Ej_sum 26.2 GHz for an in-window ~5.14 GHz arch); re-run them, or reject their persisted ej_sum_hz / f_q_max_hz / v_offset_v / v_per_phi0_v, under v0.11.1. PREREQUISITE: v0.11.0 (the drive_power_dbm combo entered below but not yet tagged) must be cut first -- bump SCQO pyproject 0.10.2->0.11.0 and tag the four repos v0.11.0 -- so the tag history and scqo --version stay consistent before v0.11.1 is cut on top."

[v0-11-0]
date = 2026-07-22
scqo = "v0.11.0"                 # drive_amp/drive_power_dbm pushed field pair; qubit_spectroscopy set->revert run()
lchqbdriver = "v0.11.0"          # LCHTransmonElement spec_amp slot; drive output_att solve; probe reads view.drive_amp
lchqmdriver = "v0.11.0"          # drive chain solve on xy full-scale grid + saturation op; probe plays amplitude_scale=1
scqo-contrib = "v0.11.0"         # no-change re-tag
scqat = "v0.1.8"                 # unchanged pin (the qubit_spectroscopy estimator consumes no amplitude/power)
notes = "Absolute drive power. qubit_spectroscopy's drive_amp Parameter (backend units: amplitude factor on QM, voltage offset on Qblox -- the same value delivered ~-21 dBm on QM chipA but ~-33 dBm on Qblox chipA) is REMOVED and replaced by drive_power_dbm (absolute dBm at the instrument drive port; default -25.0, cap +10; stale [qubit_spectroscopy] parameters.toml keys fail loudly -- values are now dBm, not a fraction). Two NEW pushed ReadableTransmon fields mirror the readout pair exactly: drive_amp (the saturation/spec drive amplitude, portable=False chain residual) declared BEFORE drive_power_dbm (portable absolute power, wins on push; coupled drive_amp echo recorded). Setters solve the DRIVE chain keeping the amplitude <= 0.5 full scale: QM picks q.xy full_scale_power_dbm on the -11..+16 dBm 3 dB grid with the saturation op amplitude as the exact residual (power_tools, operation-generic); Qblox picks the largest EVEN drive-port output_att in 0..60 with the residual on the NEW element.spec.spec_amp slot (nominal +5 dBm full scale, +/- a few dB honesty as for readout). qubit_spectroscopy.run() now does the punchout discipline: recorded boundary write of drive_power_dbm -> acquire -> exact revert in finally (2 ChangeRecords + coupled echoes per qubit); it REFUSES when a target's drive_power_dbm is unknown (unconfigured drive chain / unset saturation amplitude). The QM probe plays the saturation op at amplitude_scale=1.0; the Qblox probe plays each qubit's OWN view.drive_amp as the CW VoltageOffset (per-qubit, previously one global value). power_context gains drive-chain provenance keys (drive_full_scale_power_dbm/saturation_amp on QM; drive_output_att_db/spec_amp on Qblox; drive_power_dbm + drive LO on both). SHARED-CHAIN CAVEAT (documented in the catalog + fieldmaps): the drive chain scale is shared by every xy operation, so while it is off its standing value the stored pi_amp means a different power -- harmless in qubit_spectroscopy (no pi pulse played; the discrete chain knob + verbatim amplitude restore make the revert lossless), but scqo set QUBIT.drive_power_dbm=... IS a real re-scale of what pi_amp delivers. Both drive-chain vendor knobs flip kind vendor->realizer in the fieldmaps. UPGRADE REQUIRES (Qblox): flip each dut_config.json element_type BasicTransmonElement->LCHTransmonElement and add a 'spec': {'spec_amp': <value>} block (chipA cd1 updated in-tree with spec_amp 0.1 = the exact old default drive); a config missing the spec block still LOADS (spec_amp deserializes NaN = field unknown) but qubit_spectroscopy refuses until it is seeded (config edit or scqo set q1.drive_power_dbm=...). FluxTunableTransmonElement now subclasses LCHTransmonElement, so flux duts inherit the slot with NO edit. UPGRADE REQUIRES (QM): every qubit needs a 'saturation' xy operation (chipA has one; amp 0.2 at fs +13 = standing ~-1 dBm) -- pull-mode seeds the new fields from it, no config edit. No reinstall: no entry-point changes, git pull suffices on editables. BEHAVIOR: qubit_spectroscopy_flux now takes its OWN per-run drive_power_dbm (default -25 dBm, same recorded set->revert boundary as qubit_spectroscopy, via the shared scqo/experiments/_drive_power.py context manager): QM needed no probe change (its flux probe already played operation_amp=1.0 = the parked saturation amplitude), while the Qblox flux probe was SWITCHED from the calibrated X (pi) pulse to a weak CW saturation VoltageOffset (view.drive_amp / the spec_amp residual, cal07b pattern) -- so drive_power_dbm governs the flux map identically on both backends, and the Qblox flux map no longer needs a calibrated pi (works during bring-up). Old run folders and the index (schema v7) are unchanged; saved user parameter files carrying drive_amp are invalid (rename to drive_power_dbm, in dBm)."

[v0-10-2]
date = 2026-07-21
scqo = "v0.10.2"                 # resonator_spectroscopy gains analysis_method (lorentzian|circle) + baseline_order
lchqbdriver = "v0.10.2"          # no-change re-tag (probe-only; inherits the estimator switch)
lchqmdriver = "v0.10.2"          # no-change re-tag (probe-only; inherits the estimator switch)
scqo-contrib = "v0.10.2"         # no-change re-tag
scqat = "v0.1.8"                 # NEW: multi-method resonator estimator (joint-bg lorentzian + Probst circle)
notes = "Resonator spectroscopy analysis becomes multi-method. resonator_spectroscopy gains two Parameters: analysis_method ('lorentzian' default | 'circle') and baseline_order (0/1/2, lorentzian only). 'lorentzian' is a joint Lorentzian + polynomial-background fit of the power |IQ|^2 that REPLACES the old quantile subtract-then-fit baseline (no legacy path) -- it removes the f_res/kappa bias the value-based quiet-quantile mask produced on sloped/curved backgrounds (a horizontal band of points collapsed the order-1 baseline to nearly flat). 'circle' is a native Probst notch-model fit of the complex S21 (power-Lorentzian-seeded, multi-start over cable-delay candidates + full-model complex least-squares, delay fixed per candidate to avoid the delay/alpha/phi0 degeneracy; phase-less data gated out by a circularity test) that also yields Qi/Qc/phi0/delay; validated on real QM 5Q4C data against the vendored NCU resonator_tools (Qi within 4%, delay within 0.1 ns). The estimator + both fitters live in scqat, bumped to v0.1.8 (the combo MUST pin it -- SCQO.estimate() forwards method=/baseline_order= into scqat.estimators.resonator_spectroscopy, and the circle method + fit_notch_circle/fit_lorentzian_bg do not exist in the older v0.1.7 tag). Drivers unchanged: LCHQB/LCHQM resonator_spectroscopy is probe-only and inherits the switch, so lchqbdriver/lchqmdriver/scqo-contrib get no-change re-tags at v0.10.2 (analysis-only combo). UPGRADE REQUIRES: git fetch --tags && git checkout v0.10.2 (scqo) and scqat at v0.1.8, then re-run `uv pip install -e . --no-deps` in each editable env (refreshes the version scqo doctor reports and registers the new estimator methods). No data action: old run folders and the index (schema v7) are unchanged. BEHAVIOR: additive and non-breaking to the schema; the DEFAULT method stays 'lorentzian' so existing calls keep working, but because the baseline algorithm changed a re-fit of the SAME raw data can shift f_r/kappa slightly (more accurate on sloped backgrounds). 'circle' needs genuine phase and FAILS GRACEFULLY (outcome FAILED, no writeback) on the simulated backend whose Q quadrature is noise; on real hardware it returns f_r/kappa plus Qi/Qc. Circle's extra quality factors appear in the run result but are not yet proposed to Resonator physical fields (follow-up). RE-TAG PROVENANCE: the three driver v0.10.2 tags are no-change re-tags on the exact v0.10.1 commits (verified identical); mid-retag one command ran without an explicit cd and hit an already-tagged repo, failing harmlessly at git tag before any push -- scqo-contrib was then retagged correctly and all four scqo-versioned repos verified at v0.10.2 on origin."

[v0-10-1]
date = 2026-07-21
scqo = "v0.10.1"                 # 7 single-qubit experiments (Yuhowlin) + the drag_beta pushed field
lchqbdriver = "v0.10.1"          # drag_beta declared Unrealized (ripple from the promotion)
lchqmdriver = "v0.10.1"          # 7 QM probes + wrappers; drag_beta realized on the DragCosine node
scqo-contrib = "v0.10.1"         # no-change re-tag
scqat = "v0.1.7"                 # NEW: 6 estimators the experiments depend on (sqrb/tomography/drag x2/echo+relaxation flux)
notes = "Seven single-qubit experiments contributed by Yuhowlin, forward-ported onto the v0.10.0 component model: qubit_sqrb (randomized benchmarking), qubit_pi_pulse_error (inline fit -> refines pi_amp), qubit_tomography, qubit_drag_alternating + qubit_drag_equator (calibrate drag_beta), and qubit_echo_flux + qubit_relaxation_flux (record-only T2-echo / T1 vs flux spectra). NEW pushed ReadableTransmon field drag_beta (dimensionless DRAG coefficient): QM realizes it on the x180_DragCosine node (QUAM stores DRAG as alpha); Qblox declares it Unrealized (rxy.beta exists but no Qblox DRAG experiment writes it yet). The estimators live in scqat, bumped to v0.1.7 (the combo MUST pin it — the experiments import scqat.estimators.qubit_sqrb etc., which the older v0.1.6 tag lacks). UPGRADE REQUIRES: git fetch --tags && git checkout v0.10.1 in the four scqo-versioned repos AND scqat at v0.1.7, then re-run `uv pip install -e . --no-deps` in each editable env -- this refreshes the version string scqo doctor reports (the frozen .dist-info is not updated by git pull) and registers the new experiment/estimator modules. No data action: a device's scqo_state.json gains a drag_beta entry that seeds from the vendor in pull mode (QM) or stays unset (Qblox, Unrealized); old run folders and the index are unchanged. BEHAVIOR: additive and non-breaking; the two flux experiments (qubit_echo_flux / qubit_relaxation_flux) require FluxTunableTransmon targets declaring the flux_bias operation, so they are machine-refused pre-probe on a fixed-frequency chip. NOTE: set_pi_amp on the QM backend is now DragCosine-aware (writes the x180_DragCosine storage node) while still honoring the operation / lock_x90 contract."

[v0-10-0]
date = 2026-07-20
scqo = "v0.10.0"                 # the component model: categories.py + roster.py, per-category stores, --targets, field catalog + scqo set, placement rule, duration/integration, pairs
lchqbdriver = "v0.10.0"          # per-category field catalog; readout duration/integration knobs; qubit->target dim
lchqmdriver = "v0.10.0"          # per-category field catalog + TransmonPair view + idle_flux_v + pair_zz_coupler probe
scqo-contrib = "v0.10.0"         # no-change re-tag (combo coherence; sandbox experiments may need component-API migration)
scqat = "v0.1.6"                 # unchanged pin
notes = "The component model. A device is now a set of named COMPONENTS, each binding up to two CATEGORY slots -- a physical one (what it is: FixedTransmon/FluxTunableTransmon/Resonator/ReadoutLine/XYControl/ZControl/Coupling; fields land in physical.json) and an instrument one (how we drive it: ReadableTransmon/TransmonPair; fields land in scqo_state.json). Structure lives in a per-device components.toml (names + slots + members/topology + operations + DESIGN values); value stores stay flat numeric. A field's home is decided by the codified PLACEMENT RULE (scqo state --rule; TUTORIAL section 10). Backends declare a per-category field catalog (scqo state --fields: VendorBinding/VendorOnly, the neutral<->vendor mapping as DATA), and scqo set QUBIT.FIELD=VALUE writes an experience value with no run. Phase 2 adds two-qubit PAIRS (QCQ tunable-coupler): Coupling + TransmonPair on one name (members high/low + optional coupler satellite), the pair_zz_coupler experiment (maps residual ZZ vs coupler bias and proposes coupler_decouple_v + zz_hz), and an assignable flux_component on the flux experiments (sweep a neighbor's z or a pair's coupler; record-only). readout_duration_s + readout_integration_s promoted to neutral ReadableTransmon fields (window <= duration, 4 ns grid); idle_flux_v realized on QM (z-line flux-point offset). RENAMES (no aliases): CLI --qubits->--targets and --qubit->--component; the dataset target dim qubit->target; fit keys on NEW runs kappa_hz->kappa_tot_hz, ej_sum_ghz->ej_sum_hz (now Hz, x1e9), sweet_spot_flux_v->v_offset_v, dv_phi0_v->v_per_phi0_v, plus new f_01_hz. Store schema 2: on first contact a pre-v2 scqo_state.json/physical.json and its history sidecar are archived aside as *.v1.bak and the store reseeds (pull mode reseeds calibration from the vendor); history starts a fresh epoch (values with no visible row are expected). scqo state renders one table per category. UPGRADE REQUIRES: write a components.toml roster per device (scqo doctor hard-errors without it and prints a minimal template; component names must match the vendor config EXACTLY) -- existing chipA/chipT already carry one; rename any parameters.toml / script usage of --qubits to --targets. No reinstall: entry points unchanged, git pull + checkout suffices on editables. BEHAVIOR CHANGE: old run FOLDERS are immutable and stay findable (reindex maps the old 'qubits' record key to 'targets'; result.fit keeps its run-target keys, so trends are continuous); old datasets keep the 'qubit' dim (unread post-fit); pre-v0.10 pending suggestions are display-only (accept refuses the old format); QM stays state_sync=pull. CAVEAT: the Phase 2 PAIR path (Coupling/TransmonPair, pair_zz_coupler, idle_flux_v on flux chips) is validated by tests + simulation + OFFLINE checks against the real 5-qubit quam_state, but has NOT yet run on live pair hardware -- a pairs-only fix would be v0.10.1. scqo-contrib is a no-change re-tag for combo coherence; its sandbox experiments may still use the pre-component API and need migration."

[v0-9-0]
date = 2026-07-17
scqo = "v0.9.0"
lchqbdriver = "v0.9.0"           # hw-config null poisoning: exclude_none save + load normalization + offline compile gate
lchqmdriver = "v0.9.0"           # docs only: instrument_config is DERIVED from the registry keys
scqo-contrib = "v0.9.0"          # no-change re-tag
scqat = "v0.1.6"                 # unchanged pin
notes = "Per-(cooldown, setup) state + the operator's own values. State and physics move into each context's OWN folder <device>/<cooldown>/<setup>/scqo/ (scqo_state.json + physical.json), so two users on two setups of ONE sample never share or clobber a file; every ChangeRecord is stamped with the writing session's setup, and scqo state prints a context header saying whose numbers you are reading. ALL folder locations are DERIVED from the registry keys: a [<cycle>.setup.<name>] table is exactly backend + optional note, and the vendor folder is the sibling <cooldown>/<setup>/backend_config/ (injected by load_cooldowns as setup['instrument_config'], so factories/doctor/viewer still read one key). The sibling split keeps SCQO files out of QUAM's state-directory rglob by construction. Change history moves OUT of the values files into append-only sidecars scqo_state.history.jsonl / physical.history.jsonl (one ChangeRecord per line; new scqo/_state_io.py owns the lock + reader/writer): values files are values-only, a pre-split file's embedded history loads once and splits out on the next save, and BOTH stores now merge history under the lock -- two same-setup sessions can no longer erase each other's rows, and deleting scqo_state.json reseeds calibration WITHOUT losing provenance. NEW scqo suggest <run_id> QUBIT.FIELD=VALUE: when the estimator failed but the run's figure shows the value, attach YOUR reading to the run that justifies it as a pending origin='operator' suggestion (proposed_by/proposed_at stamped), decided through the SAME accept path and guards and credited to that run; marked [operator: <login>] in the CLI table and badged on the viewer's run page. record.json's read-modify-write is now locked (DataStore.edit_suggestions): suggest appends to the FRESH list, accept/reject replace only the rows they decided, tag_run re-reads under the lock -- concurrent writers cannot silently erase each other's proposals or decisions. scqo accept on a run with no suggestions now names scqo suggest instead of printing nothing (and short-circuits before the driver import). PhysicalStore.record refuses unknown field names (a typo used to persist silently). UPGRADE REQUIRES: hand-edit each device's cooldowns.toml -- DELETE every instrument_config line (a typed path is now refused LOUDLY, naming the derived folder) and move that setup's vendor config files into <device>/<cooldown>/<setup>/backend_config/ under the canonical names (qblox: dut_config.json + hw_config.json; qm: state.json + wiring.json). Setup names differing only by letter case are refused and cooldown ids must be filename-safe (both are folder segments now). No reinstall: no entry-point changes, git pull suffices on editables; the run index is unchanged (schema v7, no reindex). BEHAVIOR CHANGE: the per-device scqo_state.json and the device-level physical.json are RETIRED -- simply not read (fresh start); history starts fresh per context, and in pull mode calibration reseeds from the vendor config anyway."

[v0-8-0]
date = 2026-07-14
scqo = "v0.8.0"
lchqbdriver = "v0.8.0"           # readout_power_dbm via output_att solve; save() writes hw_config.json too
lchqmdriver = "v0.8.0"           # readout_power_dbm via power_tools (bidirectional full-scale)
scqo-contrib = "v0.8.0"          # no-change re-tag
scqat = "v0.1.6"                 # punchout figure: amp/chain subplot + axis-kind/mode labels (optional plotdata coords; one shared form)
notes = "Two-mode readout power. RENAME: the fast (FPGA amplitude-sweep) punchout is resonator_spectroscopy_power_amp (no alias; rename parameters.toml sections; old runs stay under the old name) -- it gains resonator_relaxation_time_ns (ring-down wait; QM overrides QUAM depletion_time, Qblox the IdlePulse) and both backends acquire amplitude->averages->frequency (frequency innermost/fastest; acquired axis order power,detuning -- scqat transposes by name). New PUSHED field readout_power_dbm (absolute dBm at the port): setters solve the output chain (QM full_scale_power_dbm -11..+16 by 3, bidirectional; Qblox output_att even 0..60 + nominal +5 dBm full scale, ±few dB) keeping the digital amplitude <= 0.5 full scale (canonical operating point); readout_amp moves as a COUPLED side effect, now recorded explicitly (ChangeRecord.coupled_to; RecordingDevice re-reads sibling pushed fields after every push so staleness/provenance stay truthful). New experiment resonator_spectroscopy_power_chain: the CAREFUL punchout (chain-stepped: python loop re-solves the chain per point at amp ~0.5, one 1D scan per point, boundary-recorded set/revert). BOTH punchouts take IDENTICAL absolute-dBm inputs (min/max_power_dbm, default -50..-20), report an ABSOLUTE dBm axis and propose readout_power_dbm + readout_freq, named for the knob each sweeps: _amp = the fast single-program FPGA amplitude sweep, realized set-top -> sweep down -> auto-revert (the qualibrate LCH pattern; boundary-recorded like _chain; prefactors shared, the QM probe module + LCH node unchanged; uniform-dBm axis on BOTH backends -- Qblox unrolls the amplitude axis point-by-point since scheduler loop domains are linear-only); _chain = the chain-stepped wide, cross-backend-comparable sweep. Each figure labels its mode and both share ONE format (map + amp/chain subplot). Both REFUSE when a qubit's readout_power_dbm is unknown (no readout_amp fallback). PARAM RENAME: _amp's min/max_power_db (relative dB) -> min/max_power_dbm (absolute); stale parameters.toml keys fail loudly. Run records gain power_context (raw chain values per qubit at run end; record.json only, index schema stays v7 -- no reindex). Qblox save() now writes hw_config.json back and syncs the dut-embedded copy. UPGRADE REQUIRES: nothing -- no entry-point changes, git pull suffices (editables); old state files load (the new field backfills from the vendor). BEHAVIOR CHANGE: first readout_power_dbm write normalizes a legacy chain to the amp<=0.5 canonical form (older pending readout_amp suggestions may go stale, truthfully); QM stays state_sync=pull."

[v0-7-0]
date = 2026-07-12
scqo = "v0.7.0"
lchqbdriver = "v0.7.0"           # factory text; ENTIRE scripts/ wrapper layer retired (wrappers, shims, launcher stubs)
lchqmdriver = "v0.7.0"           # same: scripts/ keeps only check_real_config.py
scqo-contrib = "v0.7.0"          # no-change re-tag
scqat = "v0.1.5"                 # unchanged pin
notes = "Named user-selectable setups: cooldowns.toml setups become [cycle.setup.<name>] tables (keys backend/instrument_config/note; 'since' dates + port maps retired — wiring lives in the vendor config folder; the v0.6 [[.setup]] array form refuses loudly). user.toml gains setup; scqo user --device/--setup selects (and WRITES user.toml); a single-setup cycle auto-selects, several refuse until selected. Zero-setup cycles are legal (cooldown start opens an EMPTY cycle; runs refuse until a setup block is hand-added). Runs stamp (cooldown, setup NAME); index auto-rebuilds (schema v7, old runs reindex with empty setup); scqo find --setup; accept era guard compares names. CLI regrouping: state (was device), user (new), device = admin group absorbing devices/sample/cooldown (no aliases); sync-launchers RETIRED with the drivers' whole scripts/ wrapper layer (command wrappers, _lab/_cli shims, launcher stubs) — scqo run <name> is the one way; driver scripts/ keeps only per-repo content (check_real_config.py + LCHQB's ai_loop_demo.py). UPGRADE REQUIRES: hand-edit each device's cooldowns.toml to named setup tables (fresh-start: old array form is not read); no reinstall needed. BEHAVIOR CHANGE: multi-setup cycles refuse runs until scqo user --setup."

[v0-6-0]
date = 2026-07-12
scqo = "v0.6.0"
lchqbdriver = "v0.6.0"           # scripts pass update="apply" explicitly; launcher stubs regenerated
lchqmdriver = "v0.6.0"           # same script/stub updates as the qblox driver
scqo-contrib = "v0.6.0"          # no-change re-tag
scqat = "v0.1.5"                 # unchanged pin
notes = "Suggest -> review -> accept: runs DEFAULT to suggesting fitted values (a terminal prompts; Enter applies nothing; scqo accept <run_id> decides later; --accept / update=\"apply\" = pre-v0.6 behavior; calibrate prompts per step). t1/t2 moved out of scqo_state.json into per-sample physical.json (scqo device --physical); legacy state keys not read. Index auto-rebuilds (schema v6). Live-source provenance: scqo device --sources + viewer live: markers (strict match; vendor reseeds show 'externally changed'). Guards ask [y/N] at a terminal (--force/--reapply are the script form). UPGRADE REQUIRES: nothing -- no config/data action; editable dev installs just git pull. BEHAVIOR CHANGE: runs no longer write back by default -- INSTALL section 2 v0.6.0 note."

[v0-5-0]
date = 2026-07-08
scqo = "v0.5.0"
lchqbdriver = "v0.5.0"
lchqmdriver = "v0.5.0"
scqo-contrib = "v0.5.0"          # real change: template DatasetContract fixed to the plural sweeps API
scqat = "v0.1.5"                 # independent line (v0.1.4 + version-metadata fix)
notes = "Device-centric config: user.toml picks the device; [[cycle.setup]] owns backend + instrument_config (canonical vendor filenames); instruments.toml, [qblox]/[qm] tables, [lab] backend/device_name/state_path and twin modes retired (not read). UPGRADE REQUIRES: uv pip install -e re-runs AND manual edits — shrink config.toml to [lab] data_root(+device), user.toml backend->device, cooldowns.toml [[.mapping]]->[[.setup]] + backend/instrument_config, delete instruments.toml (INSTALL §2 upgrade note). Index auto-rebuilds (schema v5); post-cooldown-end runs refuse."

[v0-4-1]
date = 2026-07-07
scqo = "v0.4.1"
lchqbdriver = "v0.4.1"
lchqmdriver = "v0.4.1"
scqo-contrib = "v0.4.1"          # no-change re-tag
scqat = "v0.1.4"                 # independent line; >= v0.1.4 required (punchout coupling)
notes = "scqo doctor; missing-driver error covers stale installs; suite isolated from real ~/.scqo; viewer tests skip without python-multipart; docs (SSH editing, server backend policy, parameters_file clarity)"

[v0-4-0]
date = 2026-07-07
scqo = "v0.4.0"
lchqbdriver = "v0.4.0"
lchqmdriver = "v0.4.0"
scqo-contrib = "v0.4.0"          # no-change re-tag
scqat = "v0.1.4"
notes = "CLI consolidation: the scqo command, scqo.backends entry points, drivers reduced to wrappers. UPGRADE REQUIRES the uv pip install -e re-runs (entry points register at install time)."
